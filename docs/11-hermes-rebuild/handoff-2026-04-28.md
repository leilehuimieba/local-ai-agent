# 交接提示词（2026-04-28）

## 项目状态

- **阶段**：阶段 I（可持续交付与工程治理）已收口，总路线 A~H 完成
- **当前**：自由迭代期，无活跃 change
- **最近一次提交**：`8896293` — 设置页扩展嵌入服务方可编辑及模块折叠

## 本次会话已完成

### 1. 设置页面可编辑性扩展（主项）

**背景**：用户反馈设置页面只能改模型/模式/工作区/开关，可用性不足。

**实现**：
- 后端 `SettingsStore.Snapshot()` 返回值从 8 个扩展到 9 个，新增 `embeddingProviderID`
- `SettingsStore.Update()` 增加 `embeddingProviderID *string` 参数
- `applySettingsUpdate` 支持 `embedding_provider_id` 字段
- `buildSettingsResponse` 优先使用 store 中的 embedding provider，fallback 到 config
- `ProviderOption` 增加 `EmbeddingModel` 字段，前端据此过滤支持嵌入的 provider
- 前端 `EmbeddingModule` 从只读卡片改为 `<select>` 下拉选择
- 前端 `SettingsActionKind` 新增 `"embedding"`，动作流与 model/workspace 切换一致

**编译状态**：前后端均通过（`go build ./...` + `npm run build`）

### 2. 知识库前端展示优化（上期遗留，已提交）

- `MarkdownContent.tsx` 轻量级 Markdown 渲染组件
- 水印/乱码 title 自动清理（`cleanTitle`/`isGarbledTitle`）
- 空摘要降级显示 content 前 120 字符
- SourcesView 增加分类/引用/标签展示

### 3. OCR 提取逻辑修复（上期遗留）

- 修复扫描版 PDF 的 bug：`pdftotext` 提取水印后清理为空，但原逻辑误判为成功，从未触发 OCR
- 修复后：清理水印后判断有效内容长度，<100 字符则强制走 OCR（百度 accurate_basic，限前 8 页）

## 活跃问题 / 待办

| 优先级 | 事项 | 状态 | 备注 |
|--------|------|------|------|
| P1 | **15 条 empty PDF 记录** | 待重试 | 百度 OCR 日调用限额已满（error_code:17），需明日额度刷新后执行 `go run ./cmd/reimport-ocr/main.go` |
| P2 | 设置页继续扩展可编辑项 | 已完成 embedding provider | 如需继续扩展，可考虑：记忆策略参数、外部连接配置等 |
| P2 | 热点文件体量监控 | 持续关注 | Rust/Go/TS 源文件 >600 行、CSS >1000 行需评估拆分 |

## 关键代码位置

| 功能 | 文件 |
|------|------|
| 设置状态存储 | `gateway/internal/state/store.go` |
| 设置路由/更新 | `gateway/internal/api/router_settings.go` |
| 设置响应构建 | `gateway/internal/api/settings_response.go` |
| 前端设置页面 | `frontend/src/settings/SettingsPanel.tsx` |
| 设置模块渲染 | `frontend/src/settings/SettingsSections.tsx` |
| 设置 API 调用 | `frontend/src/settings/useSettings.ts` |
| 动作类型定义 | `frontend/src/settings/actionRunner.ts` |
| Provider 凭证区 | `frontend/src/settings/ProviderCredentialsSection.tsx` |
| 知识库 Markdown | `frontend/src/knowledge-base/MarkdownContent.tsx` |
| 知识库工具 | `frontend/src/knowledge-base/utils.ts` |
| 文档提取/OCR | `gateway/internal/knowledge/extract.go` |

## 技术约束

- **Go 1.22+**：使用新特性，多错误用 `errors.Join`
- **Go 测试**：新增测试统一用 `testify`
- **函数行数**：新增/修改函数不得超过 30 行
- **前端**：React + Vite + TypeScript，构建命令 `npm run build`
- **设置持久化**：`data/settings/ui-state.json`
- **Embedding Provider**：切换后向量维度可能变化，需重建知识库索引（当前未自动重建，仅 UI 提示）

## 快速开始检查清单

1. 编译：`cd gateway && go build ./...` + `cd frontend && npm run build`
2. 如有修改 SettingsStore：检查所有 `Snapshot()` 调用点（当前 9 个返回值）
3. 如有修改 actionRunner：确保 `SettingsActionKind` 同步更新
4. 如有修改 contracts：检查前后端类型一致性
