# B 阶段执行记录 01：运行合同对齐与基线验证

更新时间：2026-04-10
阶段：B（进行中）

## 1. 本次目标

1. 按 A 阶段冻结文档先完成三端运行合同对齐。
2. 新增 `resume/checkpoint` 预留字段，保证向后兼容。
3. 通过构建与回归脚本验证合同改造未破坏现有链路。

## 2. 本次改动范围

1. Rust：`crates/runtime-core/src/contracts.rs`
2. Rust：`crates/runtime-core/src/lib.rs`
3. Rust：`crates/runtime-core/src/sqlite_store.rs`
4. Go：`gateway/internal/contracts/contracts.go`
5. Go：`gateway/internal/api/chat.go`
6. TS：`frontend/src/shared/contracts.ts`
7. 脚本：`scripts/run-v1-regression-check.ps1`

## 3. 关键改动

### 3.1 RunRequest 预留恢复字段

1. `resume_from_checkpoint_id`
2. `resume_strategy`

已在 Rust/Go/TS 合同中对齐。

### 3.2 RunResult 预留恢复字段

1. `checkpoint_id`
2. `resumable`

已在 Rust/Go/TS 合同中对齐。

### 3.3 兼容策略

1. Rust 使用 `Option` + `serde(default)` 保持旧响应可解析。
2. Go 使用指针字段 + `omitempty` 避免空值污染。
3. 现有执行路径默认返回 `None/nil`，不改变既有业务语义。

### 3.4 回归脚本修复

1. 已完成文档迁移后，脚本仍引用旧路径导致误报失败。
2. 已将文档检查路径改为：`docs/archive/completed-v1-20260410/`。

## 4. 验证结果

### 4.1 构建验证

1. `cargo build`：通过。
2. `go build ./...`：通过。
3. `npm run build`：通过。

### 4.2 回归验证

1. 脚本：`scripts/run-v1-regression-check.ps1`
2. 输出：`tmp/v1-regression-check/latest.json`
3. 总状态：`passed_with_warnings`
4. 唯一 warning：`sample_choco_install`（历史样本告警，脚本中定义为非阻断）

## 5. 风险观察

1. 当前 Runtime 主流程函数体较长，直接大改可能触发代码规范风险。
2. 下一步应采用“新增小函数/模块接入”的方式推进 checkpoint 实装。

## 6. 下一步（B-02）

1. 新增 checkpoint 存储模块（独立文件）。
2. 在不重写主流程的前提下接入 `checkpoint_written` 事件。
3. 增补 checkpoint 读写与兼容性测试样本。
