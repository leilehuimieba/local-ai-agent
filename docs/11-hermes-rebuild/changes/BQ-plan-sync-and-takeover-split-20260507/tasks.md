# 任务清单

- [x] 任务 1：创建 BQ change 并切换当前主推进状态
  完成判据：`current-state.md`、`changes/INDEX.md` 与 BQ 五件套同步完成。
- [x] 任务 2：新增 `plan-sync-flow-rules.ts`
  完成判据：计划同步 / 今日接管簇从 `store.ts` 外提到独立规则文件。
- [x] 任务 3：收紧 `store.ts` 装配层
  完成判据：`store.ts` 不再直接包含这簇派生细节。
- [x] 任务 4：补验证证据
  完成判据：定向与全量前端回归通过并写入 `verify.md`。
