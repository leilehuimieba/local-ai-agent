# 阶段性提审包（E-claudecode-shell-alignment）

更新时间：2026-04-14  
提审类型：阶段内收口提审（不代表 Gate-E 整体完成）

## 1. 提审范围

本次提审覆盖 `T04-T07` 及 `T07` 补刀，聚焦四条主线：

1. 工具结果合同升级（`detail_preview/raw_output_ref` 三端对齐）。
2. 命令输出双轨展示（模型侧摘要/预览 + 前端可展开原文）。
3. 权限决策链收口（规则层拆分 + ask 审计编排 + 前端状态判定校准）。
4. 压缩边界与预算补齐（会话聚合预算 + 单结果预算 + 预算命中可回放）。

## 2. 关键证据

1. 代码提交：
   - `88b7172`：`T04-T07` 对齐实现与测试补齐。
   - `f89a401`：`T07` 补刀（去除会话摘要二次截断 + 会话层测试）。
2. 文档提交：
   - `10d1f15`：本 change 文档与索引同步。
   - `aba6e7d`：状态/验证记录回写。
   - `cf4540f`：补齐 `T08` 与文档流程同步。
3. 验证命令：
   - `cargo test -p runtime-core`（113/113）。
   - `go test ./...`（gateway 通过）。
   - `npm run build`（frontend 通过）。
4. 证据索引：
   - 详见 `verify.md` 的“证据位置”与“提交记录”。

## 3. 风险与回退

1. 风险：预算与摘要策略后续若再叠加额外压缩层，可能复发“边界提示被吞”。
2. 风险：权限链字段若出现多端命名漂移，会造成前端状态误判。
3. 回退：
   - 预算链路回退：先回退 `session_prompt_summary` 最新变更，保持 compaction 主路径。
   - 权限链路回退：按 `permission_* + confirmation_*` 字段最小集合恢复展示。
   - 合同回退：以 `contracts.go` + `runtime.ts` 当前字段为最小保真基线。

## 4. 结论与建议

1. 本 change 在既定范围内已完成 `T04-T07`，并完成 `T07` 剩余缺口补刀。
2. 建议将本 change 维持“保留观察项”状态，暂不宣告 Gate-E 完成。
3. 若后续无新增任务，可按归档流程迁入 `changes/archive/<日期>/`，并在索引中保留可追溯入口。
