# 变更提案

## 背景

- 阶段 C 已通过 Gate-C 并完成收口评审，需要进入阶段 D 主推进。
- 阶段 D 目标为“记忆与技能体系”，需要先完成最小可执行骨架，避免直接进入大规模实现。

## 目标

- 启动阶段 D 首条主推进 change，冻结首批范围与验收口径。
- 先完成 `D-01`（三层记忆 schema v1 冻结）与 `D-03`（关键词检索主路径）的设计与任务拆解准备。
- 形成可直接执行的 D 阶段任务闭环入口（proposal/design/tasks/status/verify）。

## 非目标

- 本轮不直接交付 7 天 Gate-D 验收样本。
- 本轮不启用语义检索主路径，仅保留预留设计。
- 本轮不改动阶段 C 已收口的权限契约。

## 验收口径

- `D-memory-skill-foundation` 五件套齐全并入索引主推进位。
- `tasks.md` 至少包含 `D-01`、`D-03`、`D-G1` 的最小任务条目与完成判据。
