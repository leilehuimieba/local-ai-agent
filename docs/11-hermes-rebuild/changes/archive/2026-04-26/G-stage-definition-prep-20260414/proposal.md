# 变更提案

## 背景

- 本次变更要解决的问题：
  1. 阶段 F 已完成 Gate-F 签收，但当前总路线与阶段计划仅定义到阶段 F。
  2. 直接切到“阶段 G”会缺少目标/Gate/交付/回退口径，违反当前推进约束。
  3. 需要先补一份可执行的阶段 G 定义草案，再做切换决策。
- 对应阶段目标：
  1. 为“F→下一阶段”切换提供最小完备的文档输入，不做代码实现推进。

## 目标

- 本次要完成什么：
  1. 建立 `G-stage-definition-prep-20260414` 五件套并冻结范围。
  2. 输出阶段 G 草案（目标、必做任务、交付物、Gate、失败回退）。
  3. 明确切换前置条件和建议切换动作（仅文档决策，不改代码）。

## 非目标

- 本次明确不做什么：
  1. 不进入阶段 G 代码实现。
  2. 不修改 runtime-core、gateway、frontend 源码。
  3. 不覆盖并行规划项 `F-memory-progressive-disclosure-20260414`。

## 验收口径

- 通过标准：
  1. 本 change 五件套完整且状态一致（`proposal/design/tasks/status/verify`）。
  2. 形成阶段 G 定义草案文档，包含目标/Gate/交付/回退四要素。
  3. `review.md`（或等价决策结论）给出“是否可切换到阶段 G”的明确建议。
