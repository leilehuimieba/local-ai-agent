# 变更提案

## 背景

- 本次变更要解决的问题：
  1. 阶段 F 已通过 Gate-F，但总路线与阶段计划正式口径尚未完成“阶段 G”落版。
  2. `G-stage-definition-prep-20260414` 已给出草案与前置评审结论，下一步需要正式切换。
  3. 需要一次性完成“文档口径升级 + 状态切换 + 提审结论”闭环，避免阶段口径漂移。
- 对应阶段目标：
  1. 完成 `F -> G` 阶段切换签收，并建立阶段 G 的正式执行入口。

## 目标

- 本次要完成什么：
  1. 将阶段 G 正式写入总路线与阶段计划（目标/交付/Gate/失败回退）。
  2. 在最小任务总表中补齐 `G-01~G-G1`，形成阶段 G 可执行任务入口。
  3. 将 `current-state.md` 切换到“阶段 G / Gate-G（执行中）”，并同步 `changes/INDEX.md`。
  4. 产出阶段切换评审结论与验证证据。

## 非目标

- 本次明确不做什么：
  1. 不改 `runtime-core`、`gateway`、`frontend` 源码。
  2. 不执行阶段 G 的实现类任务（仅完成切换签收）。
  3. 不重跑阶段 F 的实现测试（仅引用已签收证据）。

## 验收口径

- 通过标准：
  1. `Hermes重构总路线图_完整计划.md` 与 `阶段计划总表.md` 已存在阶段 G 正式定义。
  2. `全路线最小任务分解总表.md` 已新增 `G-01~G-G1` 且状态初始化为待办。
  3. `current-state.md` 与 `changes/INDEX.md` 对齐到 `G-stage-switch-signoff-20260414`。
  4. 本 change 的 `proposal/design/tasks/status/verify/review` 已补齐且口径一致。
