# 当前阶段总整理

## 文档定位

本文件用于收拢当前并行 change 在 2026-04-22 时点的阶段性状态，方便：

1. 新开对话时快速恢复上下文
2. 向主推进侧说明这条并行 change 当前已做到哪里
3. 明确“哪些已经完成、哪些仍待补证、当前应采用什么口径”

本文件仅服务于：

- `D:/newwork/本地智能体/docs/11-hermes-rebuild/changes/H-frontend-workbench-reframe-20260421/`

不改变主推进状态，不替代：

- `D:/newwork/本地智能体/docs/11-hermes-rebuild/current-state.md`

## 一句话口径

当前这条并行 change 的推荐统一口径是：

- **前端工作台重构在代码侧已完成 Wave 1、Wave 2 与 Wave 3 的最小收口，页面截图与最小 walkthrough 已补齐；当前可结束（并行），但不切主推进。**

如果要更短一点，可以直接用：

- **代码侧已基本收口，页面证据已显著补齐。**

## 当前已完成的阶段性成果

### 1. 变更工作区已完整建立

当前并行 change 下，核心工作文档已经齐备：

1. `proposal.md`
2. `design.md`
3. `tasks.md`
4. `status.md`
5. `verify.md`

并已继续补齐：

1. `wave1-实施入口.md`
2. `wave1-深色-token-草案.md`
3. `wave1-文件级实施计划.md`
4. `wave2-实施入口.md`
5. `wave2-收口评估.md`
6. `wave3-最小入口.md`
7. `wave3-文件级实施计划.md`
8. `wave3-收口评估.md`
9. `wave3-多视图一致性检查清单.md`
10. `wave3-截图补证入口.md`
11. `wave3-保守收口口径.md`

### 2. Wave 1 已完成壳层与深色工作台基础收口

已落地到代码的文件包括：

1. `frontend/src/styles/tokens.css`
2. `frontend/src/styles/base.css`
3. `frontend/src/index.css`
4. `frontend/src/shell/AppShell.tsx`
5. `frontend/src/workspace/TopBar.tsx`
6. `frontend/src/workspace/ContextSidebar.tsx`
7. `frontend/src/workspace/BottomPanel.tsx`

当前可确认：

1. 深色工作台 token 已建立
2. AppShell 已进入稳定工作台骨架
3. 顶栏、侧栏、调查轨道已进入统一产品语义

### 3. Wave 2 已完成主线程与调查层的最小收口

已落地到代码的文件包括：

1. `frontend/src/chat/ChatPanel.tsx`
2. `frontend/src/events/EventTimeline.tsx`
3. `frontend/src/ui/primitives/InfoCard.tsx`
4. `frontend/src/ui/primitives/index.ts`

当前可确认：

1. 主线程已从“消息集合”推进为“任务流”
2. 调查层已从“信息列表”推进为“辅助轨道”
3. 共享 primitives、状态块、结果块、detail / tag 层级已完成最小收口

### 4. Wave 3 已完成附属工作区的最小收口

#### Logs

已完成：

1. `LogsPanel.tsx` 的 workbench hero
2. Logs 页区块容器收口
3. Logs workspace 语言与主任务页对齐

#### History

已完成：

1. `HistoryPageSections.tsx`
2. `HistorySpotlight.tsx`
3. `HistoryTimelineSection.tsx`
4. `HistoryDetailRail.tsx` 外层语义轻触

当前可确认：

1. review workspace 的 header / spotlight / timeline / detail rail 已形成稳定关系
2. detail rail 只做了外层语义收口，没有动内部 detail 生成逻辑

#### Settings

已完成：

1. `SettingsPanel.tsx`
2. `StatusCard.tsx`
3. `SettingsSections.tsx`

当前可确认：

1. Settings 已从传统配置页进入 workbench settings 语义

#### Resources

需要明确：

1. `resources/*` 在当前仓库里不是独立页面
2. 它挂在 Settings 视图里的“记忆与资源”模块

已完成：

1. `MemoryResourcesSection.tsx`
2. `ResourcesEntrySection.tsx`

当前可确认：

1. Resources 已按模块级进入统一工作台语言
2. 没有误改成脱离当前结构的独立页面

## 当前已经具备的验证基础

### 1. 构建验证已连续通过

当前已多次执行：

- `cd frontend && npm run build`

并在 Wave 1、Wave 2、Wave 3 各关键节点后完成复验。

### 2. 运行时阻塞已解除

已确认：

1. `scripts/start-dev.ps1` 可拉起本地 `gateway/runtime`
2. `/api/v1/settings` 已恢复为 `200`
3. 此前的 `502` 并非前端重构逻辑错误，而是 runtime/gateway 未启动导致

### 3. 已有基础证据已经落盘

当前 evidence 下已有：

1. Wave 1 首页截图
2. Wave 1 任务页截图
3. Wave 1 调查层展开态截图
4. 设置加载恢复证据
5. Wave 2 最小 walkthrough 证据
6. Wave 3 Logs workspace 截图
7. Wave 3 History / Review 视图区截图
8. Wave 3 Settings workspace 截图
9. Wave 3 Settings 内 Resources 模块截图
10. Wave 3 最小 walkthrough 证据

## 当前剩余边界

### 1. 不切主推进

当前仍需明确：

1. 当前主推进仍是 Gate-H
2. 本并行 change 不改 `current-state.md`
3. 当前不把本 change 上升表述为“前端工作台重构已全部验收”

### 2. 不继续扩大代码 diff

当前最稳妥的边界是：

1. 代码侧最小收口已经完成
2. 页面截图与最小 walkthrough 已补齐
3. 后续如需动作，以最小引用说明、交接口径或归档整理为主

## 当前推荐口径

### 对内口径

建议统一写为：

- **Wave 3 当前已完成代码侧最小收口，页面截图与最小 walkthrough 已补齐；本并行 change 可结束（并行），但不切主推进。**

### 对外或对主推进交接口径

建议统一写为：

- **本并行 change 已完成前端工作台结构重构的代码侧最小闭环，页面证据已显著补齐；当前不切主推进，后续仅保留最小引用说明。**

### 明确不要写成

1. “Wave 3 已全部完成”
2. “前端工作台重构已全部验收”
3. “当前可以切主推进”

## 当前最合理的下一步

### 优先级 1：维持保守收口口径

建议继续保持：

1. 可结束（并行）
2. 不切主推进
3. 不写成“前端工作台重构已全部验收”

### 优先级 2：如需动作，仅补最小引用说明

如果后续需要继续动作，优先做：

1. 引用 `wave3-收口评估.md`
2. 引用 `wave3-保守收口口径.md`
3. 引用 `wave3-收口交接说明.md`

### 优先级 3：不继续扩大实现范围

当前不再建议新开额外波次或继续扩代码 diff。

## 当前结论

到当前时点，这条并行 change 已经具备：

1. 完整的设计与实施文档链
2. 完整的代码侧最小收口链
3. 基本清晰的验证缺口说明
4. 稳定的保守收口口径

因此当前最稳妥的判断是：

- **这条并行 change 现在可以稳定挂住，并按“可结束（并行）”维持；不需要继续扩大代码 diff，也不切主推进。**
