# 变更提案

## 背景

- 本次变更要解决的问题：
  1. 阶段 G 已完成 `G-01~G-03`，但 `G-04` 运行手册与值守职责仍停留在初版，缺少可演练的执行频率与升级规则。
  2. 现有文档未把 `G-03` 的失败分流结果纳入值守链路，值守人面对失败样本时缺少统一动作模板。
  3. Gate-G 签收前需要把“谁在什么时间、遇到什么失败如何升级”固化为可复核口径。
- 对应阶段目标：
  1. 完成 `G-04`：运行手册与值守职责固化。

## 目标

- 本次要完成什么：
  1. 升级 `docs/11-hermes-rebuild/stage-plans/G-运行手册与值守规范.md`，补齐值守频率、值守清单、升级判定和交接产物。
  2. 新增 `G-04` 值守演练记录（`artifacts`），形成一次完整演练样本。
  3. 回写本 change 五件套，并将主推进状态切换到本 change。
  4. 更新最小任务总表，标记 `G-04` 完成，给出 `G-G1` 进入条件。

## 非目标

- 本次明确不做什么：
  1. 不进行 Gate-G 最终签收（`G-G1`）。
  2. 不改动 runtime/core 或前端功能代码。
  3. 不新增高成本自动化任务，仅收口文档与值守流程。

## 验收口径

- 通过标准：
  1. `G-运行手册与值守规范.md` 包含：执行频率、角色职责、失败升级路径、交接产物四类内容。
  2. `G-runbook-duty-closure-20260415/artifacts/` 下至少 1 份值守演练记录。
  3. 本 change 的 `tasks/status/verify/review` 与证据路径一致。
  4. `current-state/INDEX/全路线最小任务分解总表` 已同步切换并反映 `G-04` 状态。
