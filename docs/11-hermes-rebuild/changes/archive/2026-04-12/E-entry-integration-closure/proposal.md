# 变更提案

## 背景

- 阶段 E 的 `E-02`（入口协议）和 `E-04`（跨入口一致性）已完成并产出通过样本。
- 总表中的 `E-05` 仍为待办，缺口集中在“入口联调报告收口”，尤其是失败样本与回退路径的统一沉淀。
- 用户要求继续后端推进，前端窗口仍由用户并行处理，本轮不改前端代码。

## 目标

- 补齐阶段 E 的入口联调收口报告（`review.md`），统一映射成功链路、跨入口一致性链路、失败链路证据。
- 新增可复核失败样本脚本，固定产出 runtime 不可达场景的网关失败收口证据。
- 给出工程可执行的回退路径，支撑后续 `E-G1` Gate-E 评审。

## 非目标

- 本轮不改前端页面、不改前端状态消费逻辑。
- 本轮不直接宣称 Gate-E 已通过，不替代 `E-G1` 批量验收。

## 验收口径

- `changes/E-*/review.md` 已形成，且包含三类证据：入口协议成功样本、跨入口一致性样本、失败收口样本。
- 失败样本证据 `tmp/stage-e-entry-failure/latest.json` 状态为 `passed`。
- 收口文档中明确回退路径与触发条件，可直接执行复核。
