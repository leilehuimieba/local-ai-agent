# 任务清单

- [x] Q-01 从 app-views.css 提取 `.release-*` 样式到 `app-release.css`
- [x] Q-02 从 app-components.css 提取 `.model-*` 样式到 `app-model.css`
- [x] Q-03 从 app-views.css 提取 `.timeline-*` 样式到 `app-timeline.css`
- [x] Q-04 从 app-components.css 提取 `.chat-*`/`.thread-*`/`.assistant-*`/`.result-*`/`.idle-*`/`.composer-*`/`.state-*` 到 `app-chat.css`
- [x] Q-05 从 app-components.css 提取 `.task-*`/`.nav-*`/`.history-*` 到 `app-nav.css`
- [x] Q-06 从 app-views.css 提取 `.logs-*` 到 `app-logs.css`
- [x] Q-07 从 app-views.css 提取 `.settings-*` 到 `app-settings.css`
- [x] Q-08 从 app-views.css 提取 `.status-*` 到 `app-status.css`
- [x] Q-09 更新 `index.css` 导入新文件
- [x] Q-10 运行前端测试 `npm test -- --run`
- [x] Q-11 补全 verify.md
