# 任务清单

- [x] 任务 1：创建 BR change 并切换当前主推进状态
  完成判据：`current-state.md`、`changes/INDEX.md` 与 BR 五件套同步完成。
- [x] 任务 2：新增 `switch-flow-rules.ts`
  完成判据：临时切主线草稿 / 提交 / 自动恢复逻辑从 `store.ts` 外提。
- [x] 任务 3：新增 `personalized-flow-rules.ts`
  完成判据：个性化反馈草稿 / 提交 / 洞察回写逻辑从 `store.ts` 外提。
- [x] 任务 4：收紧 `store.ts` 装配层
  完成判据：`store.ts` 不再直接包含这两簇派生细节。
- [x] 任务 5：补验证证据
  完成判据：定向与全量前端回归通过并写入 `verify.md`。
