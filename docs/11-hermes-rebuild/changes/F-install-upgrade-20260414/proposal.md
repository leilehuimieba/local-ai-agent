# 变更提案

## 背景

- 本次变更要解决的问题：
  1. 阶段 E 已完成 Gate-E 签收，主推进需要切换到阶段 F。
  2. 阶段 F 首刀需收口安装/升级主路径，形成可复跑的最小产品化链路。
  3. 当前尚缺本轮阶段 F 的独立 change 工作区与验收口径。
- 对应阶段目标：
  1. 阶段 F（Windows 产品化与发布）`F-01`：安装/升级主路径实现与验证。

## 目标

- 本次要完成什么：
  1. 建立阶段 F 首刀变更范围与实现顺序（安装、升级、最小回退）。
  2. 输出可执行任务拆分与验收命令（优先 `run-stage-f-install-acceptance.ps1`）。
  3. 形成可提审的证据结构（状态、验证、失败回退）。

## 非目标

- 本次明确不做什么：
  1. 不在本刀内并行推进 `doctor` 全量能力（归属 `F-02`）。
  2. 不在本刀内完成发布候选全部回归（归属 `F-04`）。
  3. 不做 Gate-F 最终完成声明。

## 验收口径

- 通过标准：
  1. 本 change 五件套完整并可用于实施。
  2. `tasks.md` 对应 `F-01` 任务可执行且含完成判据。
  3. `verify.md` 明确安装链路验证命令、结果记录路径与回退口径。
