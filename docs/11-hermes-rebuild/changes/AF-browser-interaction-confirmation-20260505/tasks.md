# 任务清单

- [x] 任务 1：冻结 `click` / `type` contract 与风险策略
  完成判据：明确输入输出、错误码、`risk_level`、`requires_confirmation`。
- [x] 任务 2：让 confirmation 型 MCP tool 进入 request-scoped Runtime 视图
  完成判据：capability catalog 与 model tools 都能看到确认型 MCP spec。
- [x] 任务 3：打通 approval 后的 Gateway MCP 执行链
  完成判据：已批准的 `click` / `type` 不再被 Gateway 策略层二次拦截。
- [x] 任务 4：补浏览器交互最小验收证据
  完成判据：至少有一条 `open_page -> confirmation -> approved call` 的真实或接口级证据。
