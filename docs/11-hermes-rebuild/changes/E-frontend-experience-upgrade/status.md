# 当前状态

- 最近更新时间：2026-04-13
- 状态：进行中（实现态）
- 当前阶段：阶段 E - 交互与网关统一（前端重构主推进）
- 已完成：
  - 本 change 已恢复为当前前端重构主推进项，`changes/INDEX.md` 已切换活跃指向。
  - 设计窗口交接包已完成（五件套、边界冻结、Wave 1 验收口径）。
  - Wave 1 主题 1「任务页状态闭环优化」已落地到任务页主线程与调查层。
  - E-03「前端确认链字段消费改造」已完成：记录页新增审计筛选，时间线与详情栏可见确认链/工具耗时/治理字段。
  - 任务页调查层真实挂载已恢复：`renderBottomPanel` 在任务页渲染 `BottomPanel`。
  - 任务页关键场景自动展开调查层已接线：存在确认、失败或历史事件时进入任务页自动展开。
  - 主线程与调查层等待首事件文案已收口到统一映射：`chatResultModel` 提供并由两处共用。
  - 首页恢复区密度已收口：恢复区主卡聚焦“任务/阶段/摘要”，上下文与证据改为聚合卡展示。
  - 任务页主线程状态徽标已统一：头部使用“任务主线程”与统一状态徽标映射。
  - 任务页消息区层级已降噪：结果块去重、正式回答标签降噪、事件态回显替代空白态。
  - 任务页视觉基线已与首页/记录页收口：任务容器、左侧导航、主线程消息卡与状态卡统一使用 panel token（`--bg-panel/--border-default/--shadow-sm`）。
  - 任务页工具条已补齐：主线程上方增加任务标题、下一步线索、运行/会话/工作区摘要与快捷动作（调查层开关、记录页、新建任务）。
  - 任务页右侧检查器已降密度：任务态移除重复“当前任务”卡，动作/沉淀/风险卡收敛为关键字段，任务态样式改为紧凑间距。
  - 主线程消息可读性已增强：消息卡间距与内边距拉开，标题层级更清晰，结果摘要块与 action/risk/next 区块视觉权重已区分。
  - 主线程状态卡文案已压缩：状态卡正文与建议合并为单段并做重复去重，减少主线程纵向占用。
  - 主线程状态卡标题短词已收口：状态卡标题与运行态徽标统一为“完成 / 失败 / 进行中”短词体系。
  - 任务输入区交互已增强：补齐输入状态提示、清空草稿按钮与 `Esc` 快捷清空，降低误操作成本。
  - 任务页、调查层与记录页状态词汇已统一：新增统一状态词典并接入主线程状态卡、调查层摘要/事件流、记录页时间线，统一为“进行中 / 待确认 / 完成 / 失败”。
  - 主线程状态样式映射已收口：`chatResultModel` 的主线程状态 class 映射改为复用统一状态词典，减少多处分支漂移。
  - 检查器与调查层摘要状态短词已补齐：检查器“当前状态”字段与调查层摘要标题统一改为短词体系，避免“任务运行中/等待确认/任务已完成”并行口径。
  - 主线程状态标题短词化已补齐：`chatResultModel` 的状态标题函数改为统一短词输出，失败标题在有错误码时仍兼容展示。
  - 主线程状态副文案已压缩：`chatResultModel` 的 Pending/运行态正文与下一步建议改为短句表达，主线程状态卡更紧凑。
  - 主线程状态标题来源已收口：`ChatPanel` 状态卡移除无效 `title` 传参，标题统一由状态映射函数生成；`chatResultModel` 去除未消费标题函数。
  - 全局状态徽标短词已统一：`getRunStateLabel` 改为短词口径输出，TopBar 与检查器等 `statusLine` 消费位自动对齐统一词典。
  - 状态线接口已去冗余：`getRunStateLabel` 移除无效参数，`App.tsx` 状态线调用同步收口为单参数。
  - TopBar 状态色与文案已对齐：`getRunTone` 改为复用统一状态映射，`submitting` 场景不再出现“进行中文案 + 空闲色”不一致。
  - TopBar 运行占位词已统一：无 `runId` 场景从“空闲”收口为“等待中”，避免状态词汇并行。
  - 等待态词汇口径已统一：主线程等待标题、检查器动作状态、调查层摘要标题统一改为“等待中”，并保留“等待任务”样式兼容映射。
  - TopBar tone 命名已收口：确认态 tone 从 `waiting` 收口为 `awaiting`，与 `status-awaiting` 样式命名保持一致。
  - 状态样式别名已去冗余：TopBar tone 输出与状态类名完全对齐，并移除未使用 `status-waiting/status-error/status-done` 别名。
  - 检查器空闲词汇已统一：Repo 与 Context 状态在无事件时统一显示“等待中”，不再混用“空闲”。
  - 主线程播报状态已短词化：`getStreamLiveLabel` 统一播报“状态更新：短词”，并补齐归档态播报。
  - 检查器低风险状态映射已修复：`readInspectorStatusClass` 将 `low` 风险映射到 `status-awaiting`，避免“低风险待确认”回落空闲色。
  - 记录页治理状态样式已收口：`readMemoryGovernanceClass` 改为复用统一词典 class，减少与任务页并行维护。
  - 检查器状态 class 映射已单源化：`readInspectorStatusClass` 改为消费 `runtime/state` 统一词典映射，兼容同义状态由运行时集中维护。
  - 设置页状态样式映射已收口：`readModuleStatusClass` 改为消费统一词典，设置页状态 class 分支由运行时集中维护。
  - 已完成接口样本字段复核落盘：`tmp/stage-e-audit-consumption/latest.json`。
  - 已完成最小构建回归：`frontend/` 下 `npm run build` 通过。
- 进行中：
  - Wave 1 持续小步收口（以“单主题 + 最小构建回归”推进剩余体验细节）。
- 阻塞点：无。
- 下一步：
  - 继续推进 Wave 1 剩余主题，保持“每轮一个主题 + 最小构建回归”。
  - 每轮同步更新 `tasks.md / status.md / verify.md` 与对应证据路径。
