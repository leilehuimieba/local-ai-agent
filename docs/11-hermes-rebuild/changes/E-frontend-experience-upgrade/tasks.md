# 任务清单

- [x] 建立并行前端设计窗口并登记索引
  完成判据：`changes/INDEX.md` 已把本 change 加入当前活跃列表，并标注“仅方案不进实现”。
- [x] 收口前端执行口径文档
  完成判据：已对齐 `V2前端当前执行入口`、`V2前端体验方案`、`V2前端视觉方案`、`frontend-acceptance`。
- [x] 冻结本轮边界与非目标
  完成判据：明确“不新增一级页面、不改 API 语义、不做大面积重构”。
- [x] 输出 Wave 1 交接清单（实现窗口可直接执行）
  完成判据：给出文件级改动建议、顺序和每项完成判据。
- [x] 输出 Wave 1 验收清单（验收窗口可直接打分）
  完成判据：按 `通过 / 有条件通过 / 不通过` 三态给出可检查项。
- [x] 形成实现启动条件
  完成判据：明确何时允许从设计态切到实现态，并同步 `status.md` 与 `verify.md`。

## Wave 1 - 主题 1（任务页状态闭环优化）

- [x] 主线程补齐“提交后首事件前”明确等待态
  完成判据：提交任务后，在首个事件到来前，主线程显示等待态说明，不再仅靠按钮禁用反馈。
- [x] 主线程补齐“多轮任务下”首事件前等待提示
  完成判据：存在历史消息时，提交新任务后仍在主线程尾部显示等待记录，首事件到达后自动退出等待提示。
- [x] 调查层补齐自动跟随与手动查看可感知区分
  完成判据：调查层展示独立“跟随模式”提示，明确自动跟随与手动查看差异。
- [x] 调查层补齐手动查看“回到最新事件”入口
  完成判据：手动查看状态下可见“回到最新事件”按钮并可恢复自动跟随。
- [x] 失败且无事件时的主线程/调查层恢复建议
  完成判据：失败且当前轮无新事件时，主线程与调查层均显示恢复建议，不回落空态。
- [x] 完成态/失败态文案与层级可扫读性收口
  完成判据：主线程状态记录区分运行中、失败、完成三态，完成与失败文案和视觉层级可快速辨识。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 2（E-03：确认链审计字段消费）

- [x] 记录页筛选栏补齐审计维度
  完成判据：支持按 `确认链 / 工具耗时 / 治理字段` 三类审计信号筛选日志。
- [x] 时间线补齐审计标签可见性
  完成判据：时间线条目可显示确认链步骤、工具耗时、治理状态等审计标签。
- [x] 详情栏补齐确认链最小闭环字段
  完成判据：详情栏可展示 `confirmation_chain_step`、`confirmation_decision`、`confirmation_resume_strategy`、`checkpoint_id`。
- [x] 详情栏补齐工具耗时与治理字段
  完成判据：详情栏可展示 `tool_elapsed_ms`、`governance_*`、`archive_reason`。
- [x] 接口样本字段复核落盘
  完成判据：生成 `tmp/stage-e-audit-consumption/latest.json`，可复核阶段 C/D 样本中的审计字段值。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 3（任务页调查层真实挂载恢复）

- [x] 任务页恢复调查层挂载
  完成判据：`renderBottomPanel` 在任务页渲染 `BottomPanel`，不再固定返回 `null`。
- [x] 任务页调查层展开状态接线校验
  完成判据：`BottomPanel` 正确接收 `isOpen/onOpenChange/events/runState`，可响应现有联动状态。
- [x] 进入任务页关键场景自动展开调查层
  完成判据：存在确认、失败或历史事件时，进入任务页自动展开调查层，减少手动展开成本。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 4（主线程/调查层状态文案单一映射）

- [x] 统一等待首事件文案映射函数
  完成判据：在 `chatResultModel` 提供主线程与调查层共用的 `readPendingHeadline/readPendingBody/readPendingAdvice`。
- [x] 主线程改为消费统一映射
  完成判据：`ChatPanel` 不再保留本地等待态文案函数，改为调用统一映射。
- [x] 调查层改为消费统一映射
  完成判据：`BottomPanel` 不再保留本地等待态文案函数，改为调用统一映射。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 5（首页恢复区信息密度收口）

- [x] 首页恢复区关键字段降噪
  完成判据：恢复上下文主区保留“最近任务/当前阶段/最近摘要”核心字段，移除冗余运行与会话行。
- [x] 上下文摘要聚合展示
  完成判据：将会话/记忆与知识/思考聚合为两张摘要卡，降低纵向滚动成本。
- [x] 证据摘要聚合展示
  完成判据：将结果收口与验证依据聚合为单卡“结果与验证”。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 6（任务页主线程状态徽标统一）

- [x] 主线程头部文案与状态徽标统一
  完成判据：任务页主线程头部从“聊天”调整为“任务主线程”，并展示统一状态徽标样式。
- [x] 主线程状态样式映射复用
  完成判据：主线程头部状态样式复用 `readThreadStatusClass`，与调查层状态语义保持一致。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 7（任务页消息区层级降噪）

- [x] 结果块字段去重
  完成判据：助手结果区块自动去掉与摘要重复或区块间重复文本，减少同条消息内重复信息。
- [x] 正式回答标签降噪
  完成判据：默认正式回答不再额外显示“正式”标签，减少头部噪音。
- [x] 事件态回显替代空白态
  完成判据：存在事件但无消息时，主线程展示状态摘要卡，不再回落“开始一个任务”空白引导。
- [x] 状态卡头部语义统一
  完成判据：状态卡头部统一为“状态更新 + 状态徽标”，避免“完成 完成”等重复。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 8（任务页与全站视觉基线统一）

- [x] 任务页容器基线回归统一 panel 语义
  完成判据：任务页外层容器改为复用 `--bg-panel / --border-default / --shadow-sm` 视觉语义，不再使用透明裸面样式。
- [x] 主线程消息卡与状态卡视觉统一
  完成判据：主线程 `thread-record / result-block / state-record` 恢复卡片边框与背景层级，与首页/记录页一致。
- [x] 任务左侧导航视觉统一
  完成判据：左侧栏与导航面板背景、分隔线回归统一 token，移动端不再回落透明底。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 9（任务页工具条补齐）

- [x] 任务页补齐顶部工具条
  完成判据：主线程上方展示“任务标题 + 下一步线索 + 运行/会话/工作区”信息，不必依赖右侧检查器即可完成快速判断。
- [x] 任务页补齐快捷动作入口
  完成判据：工具条提供“展开/收起调查层、查看记录页、新建任务”操作，减少跨区跳转成本。
- [x] 调查层操作文案动态化
  完成判据：工具条主按钮在调查层开关状态下动态显示“展开调查层/收起调查层”，避免操作歧义。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 10（任务页检查器降密度）

- [x] 任务态检查器去重主卡
  完成判据：任务页右侧检查器不再重复展示“当前任务”卡，保留动作、沉淀、风险三张关键卡片。
- [x] 动作卡字段收敛
  完成判据：动作卡仅保留“状态/当前动作/下一步”，验证字段仅在有验证摘要时显示。
- [x] 沉淀与风险卡字段收敛
  完成判据：沉淀卡聚焦“沉淀依据/思考线索”；风险卡默认仅保留“当前阻塞/系统”，记忆字段仅在存在记忆事件时显示。
- [x] 任务态检查器紧凑样式
  完成判据：任务态检查器卡片间距与内边距较首页态更紧凑，提升主线程可见面积。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 11（主线程消息可读性增强）

- [x] 主线程消息卡节奏拉开
  完成判据：主线程消息列表间距、消息卡内间距与头部间距提升，消息扫读节奏更稳定。
- [x] 主线程标题层级增强
  完成判据：主线程标题、消息头标签、状态卡标题在字号与字重上形成清晰层级。
- [x] 结果块语义权重增强
  完成判据：结果摘要块与 action/risk/next 区块在视觉权重上可快速区分，支持“先看结论再看细节”。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 12（主线程状态卡文案压缩）

- [x] 状态卡正文合并与去重
  完成判据：主线程状态卡从“正文 + 建议”两段收敛为一段合并文案，重复句自动去重。
- [x] 状态卡正文样式压紧
  完成判据：状态卡正文字号、行高与段落间距调整为紧凑阅读节奏，减少垂直占高。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 13（状态卡标题短词收口）

- [x] 主线程状态卡标题短词统一
  完成判据：主线程状态卡标题统一为短词“完成 / 失败 / 进行中”，不再使用长句标题。
- [x] 主线程状态徽标短词统一
  完成判据：主线程状态卡徽标中的运行态词汇统一为“进行中”，与标题词汇保持一致。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 14（输入区交互增强）

- [x] 输入区状态提示补齐
  完成判据：任务输入区底部显示当前可操作提示（配置未完成、执行中、可发送、草稿可清空）。
- [x] 草稿清空入口补齐
  完成判据：有草稿且未执行时显示“清空”按钮，可一键清空输入内容。
- [x] 键盘清空快捷键补齐
  完成判据：任务输入框支持 `Esc` 快捷清空草稿，不影响回车发送任务。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 15（任务页/调查层/记录页状态词汇统一）

- [x] 建立统一状态词典映射
  完成判据：在 `runtime/state` 提供统一短词词典与 `runState` 映射，标准状态词汇统一为“进行中 / 待确认 / 完成 / 失败”。
- [x] 任务页与调查层状态词汇切换到统一词典
  完成判据：任务页主线程状态卡、任务工具条标签、调查层摘要状态统一使用词典映射，不再出现“运行中/已完成/执行失败”等并行口径。
- [x] 记录页与调查层时间线状态词汇切换到统一词典
  完成判据：记录页时间线与调查层事件流状态徽标统一使用词典映射，状态词汇与任务页保持一致。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 16（主线程状态样式映射收口）

- [x] 主线程状态样式映射改为复用统一词典
  完成判据：`chatResultModel` 的 `readThreadStatusClass` 不再本地分支判断，改为消费 `runtime/state` 的统一映射。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 17（检查器与调查层状态短词补齐）

- [x] 检查器“当前状态”字段切换统一短词
  完成判据：任务页与首页态检查器的“当前状态”字段统一显示“进行中 / 待确认 / 完成 / 失败”短词体系。
- [x] 调查层摘要标题切换统一短词
  完成判据：调查层摘要标题由长句状态改为统一短词，失败态仍保留失败级别语义。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 18（主线程状态标题短词化）

- [x] 状态标题函数改为统一短词输出
  完成判据：`chatResultModel` 中 `readRunStateHeadline/readPendingHeadline` 统一输出“进行中 / 待确认 / 完成 / 失败”等短词，不再输出长句标题。
- [x] 失败标题兼容错误码
  完成判据：失败态在有 `error_code` 时仍可显示错误码，否则回落“失败”短词，正文继续承载详细说明。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 19（主线程状态副文案压缩）

- [x] Pending 文案短句化
  完成判据：`readPendingBody/readPendingAdvice` 改为短句表达，保持“等待首个事件”和“自动切回最新焦点”语义不变。
- [x] 运行态正文与下一步短句化
  完成判据：`readRunStateBody/readRunStateNextStep` 在 submitting/streaming/awaiting_confirmation/resuming/completed 场景改为更短表达，减少状态卡文案密度。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 20（主线程状态标题来源收口）

- [x] 状态卡标题移除无效传参
  完成判据：`ChatPanel` 的 `StateRecord` 移除未生效 `title` 传参，状态标题仅由统一状态映射生成。
- [x] 状态标题函数去冗余
  完成判据：`chatResultModel` 删除未被消费的 `readRunStateHeadline`，避免“函数存在但无渲染入口”漂移。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 21（全局状态徽标短词统一）

- [x] `statusLine` 输出改为短词口径
  完成判据：`runtime/state` 的 `getRunStateLabel` 统一输出短词状态（等待中/进行中/待确认/完成/失败），不再返回“等待首次任务/空闲/提交中”等并行口径。
- [x] TopBar/检查器状态徽标自动对齐
  完成判据：依赖 `statusLine` 的顶栏与检查器状态徽标自动对齐统一词典，无需额外分支改造。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 22（状态线接口去冗余）

- [x] 去除 `getRunStateLabel` 无效参数
  完成判据：`runtime/state` 的 `getRunStateLabel` 移除未使用 `eventCount` 参数，函数签名与行为一致。
- [x] 状态线调用点同步
  完成判据：`App.tsx` 的 `statusLine` 计算调用同步改为单参数，保持运行行为不变。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 23（TopBar 状态色与文案对齐）

- [x] `getRunTone` 改为复用统一状态映射
  完成判据：`runtime/state` 的 `getRunTone` 基于统一状态词典计算，`submitting` 不再回落 `idle` 色。
- [x] TopBar 状态色与短词文案一致
  完成判据：TopBar 在 `submitting/streaming/resuming` 场景下统一显示“进行中”且使用 `status-running` 语义色。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 24（TopBar 运行占位词汇统一）

- [x] TopBar 运行占位词改为统一口径
  完成判据：TopBar 的“运行”字段在无 `runId` 时从“空闲”改为“等待中”，与全局状态短词一致。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 25（等待态词汇口径统一）

- [x] 运行态等待词统一为“等待中”
  完成判据：主线程等待标题、检查器动作状态、调查层摘要标题中的运行态等待词统一为“等待中”。
- [x] 旧词样式兼容映射保留
  完成判据：检查器状态样式映射保留“等待任务”兼容分支，避免历史数据回放样式异常。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 26（TopBar tone 命名收口）

- [x] TopBar tone 命名对齐 `status-awaiting`
  完成判据：`getRunTone` 的确认态 tone 输出从 `waiting` 收口为 `awaiting`，与主样式命名保持一致。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 27（状态样式别名去冗余）

- [x] TopBar tone 输出与状态类名完全对齐
  完成判据：`getRunTone` 输出统一为 `idle/running/awaiting/completed/failed`，不再使用 `done/error` 别名。
- [x] 未使用状态样式别名移除
  完成判据：样式中移除 `status-waiting/status-error/status-done` 未使用别名，仅保留统一状态类名。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 28（检查器空闲词汇统一）

- [x] 检查器 Repo/Context 空闲词汇改为等待中
  完成判据：任务页检查器中 Repo 与 Context 状态在无事件时统一显示“等待中”，不再混用“空闲”。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 29（主线程播报状态短词化）

- [x] `aria-live` 状态播报收口到统一短词
  完成判据：`getStreamLiveLabel` 在运行中/待确认/完成/失败场景统一播报“状态更新：短词”，不再使用长句播报。
- [x] 归档态播报补齐
  完成判据：`archived` 场景可播报“状态更新：已归档”。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 30（检查器低风险状态映射修复）

- [x] 低风险待确认状态映射修复
  完成判据：`ContextSidebar` 的检查器状态样式映射中，`low` 风险状态映射到 `status-awaiting`，不再误落 `status-idle`。
- [x] 低风险词汇样式与短词口径一致
  完成判据：检查器风险状态在“待确认”语义下统一显示待确认色，与统一状态词典口径一致。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 31（记录页治理状态样式收口）

- [x] 记录页治理状态 class 复用统一词典
  完成判据：`history/logType.ts` 的 `readMemoryGovernanceClass` 改为通过统一状态词典返回 class，不再本地硬编码 `status-*` 字符串。
- [x] 治理状态完成/待治理/空闲语义保持一致
  完成判据：治理状态在“完成/待治理/默认”场景分别命中统一词典的 `completed/awaiting_confirmation/idle`，视觉语义不变。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 32（检查器状态 class 映射单源化）

- [x] 检查器状态 class 映射改为消费统一词典
  完成判据：`ContextSidebar` 的 `readInspectorStatusClass` 改为通过 `runtime/state` 的状态词典映射返回 class，不再维护本地同义词分支。
- [x] 文案别名兼容迁移到运行时词典
  完成判据：`失败/降级`、`待确认/high/medium/low`、`完成/已归档`、`等待中/空闲` 等兼容标签统一由运行时词典处理。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 33（设置页状态样式映射收口）

- [x] 设置页模块状态 class 改为复用统一词典
  完成判据：`SettingsSections` 的 `readModuleStatusClass` 改为消费 `runtime/state` 统一词典，不再本地维护 `status-*` 分支映射。
- [x] 设置页状态别名纳入运行时词典
  完成判据：`已锁定/未启用/已启用` 等设置页状态别名由运行时词典统一映射，设置页与任务页语义一致。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 34（Provider 状态样式映射收口）

- [x] Provider 区域状态 class 改为复用统一词典
  完成判据：`ProviderCredentialsSection` 的 `readProviderModuleBadge/readProviderPill` 改为通过统一词典映射 class，不再本地硬编码 `status-*`。
- [x] Provider 状态别名纳入运行时词典
  完成判据：`已应用/待应用/已保存未应用/未配置` 等 Provider 专用状态标签由运行时词典统一映射。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 35（状态总览卡样式映射收口）

- [x] 状态总览卡 class 映射改为复用统一词典
  完成判据：`StatusCard` 的 `readOverallStatusClass` 改为消费 `runtime/state` 统一词典，不再本地维护 `status-*` 分支。
- [x] 状态总览卡断连特例保留
  完成判据：`已断开` 状态继续命中 `status-disconnected`，其余状态由统一词典映射，保持既有语义色。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 36（Provider 就绪语义色修正）

- [x] Provider “就绪”状态语义色回归
  完成判据：`ProviderCredentialsSection` 在“就绪”标签场景继续使用空闲色，不因词典收口误命中完成色。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 主题 37（Provider 状态键映射显式化）

- [x] Provider 状态 class 改为按状态键映射
  完成判据：`ProviderCredentialsSection` 不再按文案反推状态，改为按 `idle/awaiting_confirmation/completed/failed` 明确状态键映射 class。
- [x] 最小构建回归
  完成判据：在 `frontend/` 目录执行 `npm run build` 并通过。

## Wave 1 - 后续待推进任务池（主题 38+）

- [x] 主题 38（设置页 Provider 反馈态样式收口）
  完成判据：`ProviderCredentialsSection` 的反馈提示（pending/error/success）状态 class 与统一词典语义对齐，不再依赖局部 tone 到 class 的隐式映射。
- [x] 主题 39（设置页状态映射消费口径巡检）
  完成判据：设置页内所有状态徽标消费点完成一次巡检，新增“已收口/待收口”清单，避免遗漏并行口径。
- [x] 主题 40（设置页 ExternalConnection 状态映射收口）
  完成判据：`SettingsSections.readExternalConnectionModel` 去除 `status-*` 直写分支，改为统一词典消费并保持“已可用/当前受限/已预留未接入/未绑定工具”语义色一致。
- [x] 主题 41（调查层事件状态别名清理）
  完成判据：调查层事件流中仅保留必要历史兼容状态别名，其余无来源同义分支移除。
- [x] 主题 42（记录页状态徽标映射收口补齐）
  完成判据：记录页时间线与详情栏中剩余本地状态 class 分支收口到统一词典，跨页状态语义一致。
- [x] 主题 43（主线程状态卡密度二次压缩）
  完成判据：主线程状态卡在不损失关键信息前提下再压一层文案密度，保障首屏可见条目数提升。
- [x] 主题 44（任务页移动端布局压测）
  完成判据：任务页在窄屏下完成布局压测，修复工具条换行、状态卡挤压、调查层切换按钮点击区问题。
- [x] 主题 45（状态词典收口阶段验收包）
  完成判据：补齐状态词典收口阶段验收记录（覆盖页、映射点、回归结果、已知遗留），作为进入下一波优化的基线。
- [x] 主题 46（记录页结构化筛选增强）
  完成判据：记录页筛选维度按“结果/风险/验证/治理”分组优化，筛选与详情联动路径可一键复查。
- [ ] 主题 47（设置页诊断链路可读性增强）
  完成判据：设置页诊断结果分组展示“当前状态/影响范围/建议动作”，并与状态词典语义保持一致。
- [ ] 主题 48（前端重构阶段性回归脚本清单）
  完成判据：形成前端最小回归脚本与人工检查项清单，后续每刀可复用，不再临时拼接验证项。
