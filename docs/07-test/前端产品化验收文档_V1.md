# 本地智能体 - 前端产品化验收文档 V1

更新时间：2026-04-03

状态：`当前有效`

执行标记：

1. `当前前端产品化阶段正式验收文档`
2. `实现完成后必须按本文逐项填写`
3. `是否允许进入产品级总验收阶段，以本文结论为准`

关联文档：

1. [V2前端当前执行入口_V1](D:/newwork/本地智能体/docs/10-v2/V2前端当前执行入口_V1.md)
2. [V2前端视觉与界面实现方案_V1](D:/newwork/本地智能体/docs/10-v2/V2前端视觉与界面实现方案_V1.md)
3. [V2产品需求文档](D:/newwork/本地智能体/docs/10-v2/V2产品需求文档.md)

---

## 1. 文档目的

本文只回答 4 个问题：

1. 三视图前端是否已经达到当前 V2 文档要求
2. 首页、日志页、设置页是否已形成统一工作台语言
3. 当前真实功能是否在前端重构后保持可用
4. 是否允许进入产品级总验收阶段

本文件不是新的开发任务书。
本文件是当前前端产品化阶段的正式验收口径。

---

## 2. 填写说明

实现完成后必须按以下规则填写：

1. 每一节都要写
2. 必须写清楚“做了什么、没做什么、怎么验证的”
3. 必须给出文件落点
4. 必须写明构建结果和手工验证结果
5. 必须给出明确结论：`通过 / 有条件通过 / 不通过`

---

## 3. 本轮目标

实际填写：

1. `把首页、日志页、设置页收口为统一的本地 AI 智能体工作台。`
2. `保证首页像任务工作台、日志页像调查页、设置页像运行时控制面板。`
3. `在不破坏聊天、确认、事件、日志、模型切换、工作区切换主链路的前提下完成前端成熟化第一轮验收。`
4. `完成模式体系专项复核，并把日志筛选与详情联动接成真实功能。`
5. `把设置页中的 Memory Policy / Diagnostics 从占位模块转成真实模块。`

---

## 4. 实现范围

实际填写：

1. `首页固定为工作台页，主区保留任务概览，右侧接入 Task Hub，底部保留 Investigation Layer。`
2. `对话主线程迁移到独立 Agent Page，避免首页再次膨胀成聊天页。`
3. `日志页升级为“日志与回放”，包含摘要区、真实筛查工具条、时间线与详情区联动。`
4. `设置页升级为运行时控制面板，分为 Runtime Environment / Model Providers & Routing / Workspace Roots / Risk & Permissions / Memory Policy / Diagnostics & Utilities。`
5. `补齐首页、对话页、日志页、设置页的最小空状态与错误提示。`

---

## 5. 修改文件清单

实际填写：

1. `frontend/src/App.tsx`
2. `frontend/src/index.css`
3. `frontend/src/workspace/TopBar.tsx`
4. `frontend/src/workspace/WorkbenchOverview.tsx`
5. `frontend/src/workspace/ContextSidebar.tsx`
6. `frontend/src/workspace/BottomPanel.tsx`
7. `frontend/src/chat/ChatPanel.tsx`
8. `frontend/src/confirmations/ConfirmationCard.tsx`
9. `frontend/src/events/EventTimeline.tsx`
10. `frontend/src/logs/LogsPanel.tsx`
11. `frontend/src/settings/SettingsPanel.tsx`
12. `frontend/src/settings/StatusCard.tsx`
13. `crates/runtime-core/src/tools.rs`
14. `crates/runtime-core/src/context_builder.rs`
15. `frontend/src/settings/api.ts`
16. `frontend/src/shared/contracts.ts`
17. `gateway/internal/api/router.go`

---

## 6. 页面结构验收

### 6.1 正式页面边界

必须说明：

1. 是否仍只保留首页、日志页、设置页三条正式产品主线
2. 是否没有扩成新的重型页面体系

实际填写：

1. 正式页面：`通过。当前正式入口为首页工作台、独立对话页、日志页、设置页；其中产品主线仍围绕首页、日志、设置三页组织，对话页只承接主聊天线程，不扩成新产品域。`
2. 边界控制：`通过。未新增 skills、记忆页、多智能体页、IDE 式对象树导航或重型运维控制台首页。`

### 6.2 首页工作台结构

必须说明：

1. 首页是否明显像任务工作台
2. 主区、右栏、底部调查层关系是否清晰

实际填写：

1. 工作台形态：`通过。首页主区已收口为任务概览与调查入口，不再承载大段聊天内容。`
2. 结构关系：`通过。主区为 WorkbenchOverview，右侧为 Task Hub，底部为 Investigation Layer，三者职责边界清晰。`

### 6.3 日志页结构

必须说明：

1. 是否已经不是简单日志列表
2. 是否具备复盘与排障结构

实际填写：

1. 页面形态：`通过。日志页已升级为“日志与回放”，不再只是列表。`
2. 调查结构：`通过。当前包含页面头部、摘要统计、真实筛查工具条、主时间线区和详情联动区。`

### 6.4 设置页结构

必须说明：

1. 是否已经不是普通表单集合
2. 是否已形成运行时控制面板结构

实际填写：

1. 页面形态：`通过。设置页已按运行时控制面板收口，不再是单纯表单堆叠。`
2. 模块结构：`通过。已具备 Runtime Environment / Model Providers & Routing / Workspace Roots / Risk & Permissions / Memory Policy / Diagnostics & Utilities 六大区块。`

---

## 7. 首页工作台验收

### 7.1 顶栏与应用框架感

必须说明：

1. 顶栏是否像应用框架头部
2. 当前模型、模式、工作区、会话状态是否轻量可见

实际填写：

1. 顶栏形态：`通过。左 rail 已收口为应用框架头部与导航壳层，不再是普通顶部条。`
2. 状态展示：`通过。当前模型、运行模式、工作区、会话、运行状态均可直接看到。`

### 7.2 右侧 Task Hub

必须说明：

1. 是否已真实接入首页
2. 是否围绕任务中枢而不是卡片拼盘组织

实际填写：

1. 接入情况：`通过。ContextSidebar 已真实挂入首页布局，不再是代码存在但页面未使用。`
2. 信息组织：`通过。Task Hub 固定围绕当前任务、下一步、最近动作、仓库上下文、风险确认、记忆痕迹组织。`

### 7.3 底部 Investigation Layer

必须说明：

1. 是否只保留当前任务事件流
2. 是否与独立日志页形成分层

实际填写：

1. 内容边界：`通过。BottomPanel 已去掉 logs tab，只保留当前任务事件流。`
2. 分层关系：`通过。首页底部只负责当前任务调查，稳定日志统一进入独立日志页。`

### 7.4 空状态与异常态

必须说明：

1. 首次进入时是否有最小引导
2. Runtime 不可达或设置未加载时是否有明确提示

实际填写：

1. 首次引导：`通过。首页、对话页、日志页均提供最小空状态文案，引导用户从第一条真实任务开始。`
2. 异常提示：`通过。首页和右侧 Task Hub、设置页都已补 Runtime 不可达与设置加载中的可理解提示。`

---

## 8. 日志页验收

### 8.1 调查与回放结构

必须说明：

1. 是否具备摘要区
2. 是否具备时间线主体与详情区

实际填写：

1. 摘要区：`通过。日志页保留总记录量、错误数量、风险确认、涉及工具四类摘要卡。`
2. 主体结构：`通过。左侧为时间线主体，右侧为详情查看位与调查摘要卡。`

### 8.2 日志筛选与详情联动

必须说明：

1. 是否已经接入真实筛选逻辑
2. 左侧时间线与右侧详情是否真实联动

实际填写：

1. 筛选逻辑：`通过。已接入关键词、级别、重点三类真实筛选，支持 all/error/warn/info 与 risk/tool/memory 组合筛查，不再是占位结构。`
2. 详情联动：`通过。点击左侧时间线记录后，右侧详情区会切换到对应日志，并直接展示 context_snapshot、verification_snapshot、error 等正式字段。`

### 8.3 详情消费能力

必须说明：

1. 是否直接展示关键上下文与验证快照
2. 是否减少对 metadata 的纯依赖

实际填写：

1. 快照展示：`通过。详情区可直接展示 context_snapshot.workspace_root、memory_digest、verification_snapshot.summary。`
2. 依赖关系：`通过。当前详情区已优先消费 context_snapshot / verification_snapshot / error 等正式字段，不再只看 metadata。`

### 8.4 模式体系专项复核

必须说明：

1. 三种模式的边界是否已明确落到代码与设置页
2. 是否仍存在“模式只是文案切换”的情况

实际填写：

1. 边界落点：`通过。runtime-core 已在 tools.rs 中按模式控制可见工具范围，并在 context_builder.rs 中输出明确中文模式提示；设置页也同步展示 observe / standard / full_access 的正式边界说明。`
2. 行为真实性：`通过。observe 继续屏蔽修改性工具，standard 允许常见开发读写但屏蔽高级写入能力，full_access 开放全部已注册能力但高危删除和危险命令仍保留人工确认。`

---

## 9. 设置页验收

### 9.1 真实可用模块

必须说明：

1. 当前已有真实功能的模块有哪些
2. 是否允许占位模块保持“后续接入”状态

实际填写：

1. 真实模块：`通过。Runtime、模型切换、模式切换、工作区切换、目录授权、风险开关、Memory Policy、Diagnostics 都已接入真实状态或真实动作。`
2. 占位模块：`通过。当前设置页不再把 Memory Policy 与 Diagnostics 标成“后续接入”；剩余未接入的仅是更细粒度的策略开关，不会伪装成已完成。`

### 9.2 风险与授权体验

必须说明：

1. 工作区与授权能力是否在设置页集中管理
2. 风险开关是否真实生效

实际填写：

1. 集中管理：`通过。Workspace Roots 与 Risk & Permissions 已承担完整管理职责。`
2. 真实生效：`通过。目录提醒开关、风险等级展示开关都已在前一阶段完成正式联调验收。`

### 9.3 Memory Policy 与 Diagnostics 转正

必须说明：

1. Memory Policy 是否已展示真实状态与落点
2. Diagnostics 是否已具备真实工具动作

实际填写：

1. Memory Policy：`通过。设置页现已展示记忆启用状态、召回策略、写入策略、清理策略、长期记忆数量、知识条目数量、工作记忆文件数，以及 SQLite / working_memory / long_term_memory / knowledge_base 的真实落点。`
2. Diagnostics：`通过。设置页现已支持导出运行日志、导出当前配置、打开诊断摘要 JSON，并展示 settings / run-logs / run-events / repo_root 等真实诊断路径与运行时状态。`

---

## 10. 主链路回归验收

必须说明：

1. 聊天主链路是否仍可用
2. 确认流、事件流、日志页、模型切换、工作区切换是否未被改坏

实际填写：

1. 聊天主链路：`通过。独立 Agent Page 继续承接提交任务、展示消息、展示确认卡和错误提示。`
2. 关键回归：`通过。确认流、事件流、全局日志、模型切换、模式切换、工作区切换均保持可用，没有因为前端重构失效。`

---

## 11. 手工验证记录

至少覆盖 4 类场景：

### 11.1 场景一：首页工作台

1. 输入：`进入首页工作台`
2. 预期：`主区、右侧 Task Hub、底部调查层结构清晰`
3. 实际结果：`通过。首页已形成“概览主区 + Task Hub + Investigation Layer”结构，右侧与底部之间语义联动成立。`

### 11.2 场景二：日志页复盘

1. 输入：`进入日志页`
2. 预期：`看到摘要统计、筛查工具条、时间线、详情区，并可联动切换`
3. 实际结果：`通过。日志页可按关键词、级别、重点筛查日志，点击左侧记录后右侧详情区会切换展示对应上下文快照、验证快照和错误信息。`

### 11.3 场景三：设置页控制面板

1. 输入：`进入设置页`
2. 预期：`看到运行时控制面板结构与工作区/风险设置`
3. 实际结果：`通过。设置页已具备六大模块区块，工作区、授权、风险、模型、模式都可操作。`

### 11.4 场景四：首次进入与错误提示

1. 输入：`无任务事件或 Runtime 不可达时查看首页/设置页`
2. 预期：`能看到最小引导与明确错误提示`
3. 实际结果：`通过。首页、Task Hub、对话页、设置页都已补最小引导或 Runtime 异常提示。`

### 11.5 场景五：模式边界复核

1. 输入：`切换 observe / standard / full_access 并查看设置页与运行时模式提示`
2. 预期：`三种模式边界一致，且 full_access 仍保留高危确认`
3. 实际结果：`通过。设置页、运行时模式提示与工具可见性控制一致；observe 为只读边界，standard 为常见开发读写，full_access 为全部能力开放但不绕过高危确认。`

### 11.6 场景六：Memory Policy / Diagnostics

1. 输入：`进入设置页，查看 Memory Policy，并点击导出运行日志 / 导出当前配置 / 打开诊断信息`
2. 预期：`看到真实记忆状态与路径，并执行真实诊断动作`
3. 实际结果：`通过。Memory Policy 已展示真实计数、策略说明和存储落点；Diagnostics 已可导出日志、导出配置并打开诊断摘要 JSON。`

---

## 12. 构建与运行验证

实际填写：

1. `npm run build`：`通过。2026-04-03 在 frontend/ 执行 npm run build，Vite 构建完成。`
2. `go build ./...`：`通过。2026-04-03 在 gateway/ 执行 go build ./...，无报错。`
3. `cargo build`：`通过。2026-04-03 在仓库根目录执行 cargo build，构建完成。`

---

## 13. 偏离任务书的地方

实际填写：

1. `当前首页保留了独立对话页作为主聊天承载，不再坚持把所有任务线程都留在首页；这是为降低首页信息密度而做的受控调整。`
2. `Memory Policy 与 Diagnostics 本轮按“真实状态 + 真实动作”转正，但更细粒度的记忆策略开关和高级诊断动作仍未继续扩写。`

---

## 14. 未完成项

### 14.1 阻断项

1. `无`

### 14.2 非阻断项

1. `Memory Policy 目前展示的是正式状态摘要与存储落点，还没有更细粒度的策略编辑能力。`
2. `Diagnostics 目前已具备导出和摘要查看，但还没有更深入的一键健康检查或自动诊断能力。`
3. `空状态、错误态与首次使用引导已具备最小可用形态，但仍可继续优化成更完整的产品文案体系。`

---

## 15. 验收结论

### 15.1 当前正式结论

1. 结论：`通过`
2. 验收日期：`2026-04-03`
3. 验收人：`Codex`

### 15.2 结论理由

1. `首页、日志页、设置页已经形成统一视觉语言，且都符合当前 V2 文档的页面职责。`
2. `首页已明显像任务工作台，右侧 Task Hub 和底部 Investigation Layer 都已真实接入并形成语义联动。`
3. `日志页与设置页已经从“列表/表单集合”升级为“日志与回放页 / 运行时控制面板”，主链路未被改坏。`
4. `模式体系专项复核已完成，日志筛选与详情联动也已接成真实功能，前端阶段原先的两项核心遗留已收口。`
5. `Memory Policy 与 Diagnostics 也已从占位转成真实状态与真实动作，前端产品化阶段核心收口项已全部完成。`

---

## 16. 是否允许进入下一阶段

1. 结论：`允许`
2. 原因：`本阶段结论为“有条件通过”，遗留问题均为非阻断项，不影响进入产品级总验收阶段。`
