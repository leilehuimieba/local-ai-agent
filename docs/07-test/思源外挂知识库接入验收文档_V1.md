# 本地智能体 - 思源外挂知识库接入验收文档 V1

更新时间：2026-04-02

状态：`当前有效`

执行标记：

1. `当前思源接入阶段正式验收模板`
2. `实现完成后必须按本文逐项填写`
3. `是否允许进入下一收口阶段，以本文结论为准`

关联文档：

1. [思源外挂知识库接入方案_V1](D:/newwork/本地智能体/docs/06-development/思源外挂知识库接入方案_V1.md)
2. [思源外挂知识库接入开发任务书_V1](D:/newwork/本地智能体/docs/06-development/思源外挂知识库接入开发任务书_V1.md)
3. [SQLite 主存储收口方案_V1](D:/newwork/本地智能体/docs/06-development/SQLite%20主存储收口方案_V1.md)

---

## 1. 文档目的

本文只回答 4 个问题：

1. 思源是否已经作为外挂知识库正式接入
2. 思源是否已和 `SQLite` 主索引层稳定协作
3. 当前实现是否允许进入下一收口阶段
4. 还剩哪些遗留项

本文件不是新的开发任务书。
本文件是当前阶段的正式验收口径和填写模板。

---

## 2. 填写说明

实现完成后必须按以下规则填写：

1. 每一节都要写
2. 必须写清楚“做了什么、没做什么、怎么验证的”
3. 必须给出文件落点
4. 必须写明构建结果和手工测试结果
5. 必须给出明确结论：`通过 / 有条件通过 / 不通过`

---

## 3. 本轮目标

实际填写：

1. `把思源正式接入为外挂知识库，补齐摘要搜索、正文读取、知识导出、摘要回写 SQLite 与来源展示。`
2. `保证 SQLite 仍是主索引层，思源只承接外挂正文层和导出落点，不侵入短期工作记忆与日志事件体系。`

---

## 4. 实现范围

实际填写：

1. `在 crates/runtime-core/src/paths.rs、gateway/internal/config/config.go、gateway/internal/api/chat.go、config/app.json 增加思源配置入口。`
2. `在 crates/runtime-core/src/planner.rs、crates/runtime-core/src/tools.rs、crates/runtime-core/src/execution.rs 增加 search_siyuan_notes、read_siyuan_note、write_siyuan_knowledge 三类能力。`
3. `在 crates/runtime-core/src/memory_router.rs 与 crates/runtime-core/src/execution.rs 增加思源导出和摘要回写 SQLite 的逻辑。`
4. `在 crates/runtime-core/src/knowledge.rs 保持 SQLite 主查询前提下，增加思源检索回退与索引协作。`
5. `在 frontend/src/events/EventTimeline.tsx 与 frontend/src/logs/LogsPanel.tsx 增加 source_type = siyuan 的最小来源展示。`

---

## 5. 修改文件清单

实际填写：

1. `crates/runtime-core/src/planner.rs`
2. `crates/runtime-core/src/tools.rs`
3. `crates/runtime-core/src/execution.rs`
4. `crates/runtime-core/src/memory_router.rs`
5. `frontend/src/events/EventTimeline.tsx`

---

## 6. 接入边界验收

### 6.1 思源角色定位

必须说明：

1. 思源是否仍为外挂知识库
2. 是否没有侵入短期工作记忆与主数据库角色

实际填写：

1. 角色定位：`思源当前仍是外挂知识库，不是主数据库，也不是短期工作记忆容器。`
2. 边界情况：`边界保持住了。短期工作记忆仍落在 data/working_memory；长期记忆主表仍在 SQLite；日志、事件、artifact 仍是 JSONL。`

### 6.2 SQLite 协作关系

必须说明：

1. 查询主链路是否仍优先走 `SQLite`
2. 思源是否只承担正文层与外挂知识层

实际填写：

1. 查询主链路：`查询主链路仍优先走 SQLite 与工作区文件命中，思源没有替代 SQLite 主索引角色。`
2. 思源职责：`思源只承担外挂正文文件与导出目录角色，同时把摘要回写为 SQLite 索引项，source_type 标记为 siyuan。`

---

## 7. 核心能力验收

### 7.1 思源摘要搜索

必须说明：

1. 是否支持按关键词搜索思源条目
2. 返回是否为标题、摘要、来源

实际填写：

1. 搜索能力：`已支持。执行 “思源检索: 写入思源” 可命中导出 md 文件。`
2. 返回内容：`返回标题所在路径与摘要片段，不直接返回全文；实际返回包含 2 条 exported 目录下的命中结果。`

### 7.2 思源正文读取

必须说明：

1. 是否支持按命中结果读取正文
2. 是否只在需要时展开正文

实际填写：

1. 正文读取：`已支持。执行 “读取思源: D:/newwork/本地智能体/data/siyuan/exported/main-run-siyuan-verify-3.md” 可直接读取正文。`
2. 按需展开：`已按需展开。只有在 read_siyuan_note 显式命中后才读取正文，search_siyuan_notes 只返回摘要。`

### 7.3 知识导出到思源

必须说明：

1. 是否支持把高价值知识导出到思源
2. 导出结构是否稳定

实际填写：

1. 导出能力：`已支持。执行 “写入思源” 后会导出 md 文件到思源导出目录。`
2. 导出结构：`当前导出为稳定 md 文件，路径规则为 data/siyuan/exported/{workspace_id}-{run_id}.md，文件内容为标题 + 摘要正文。`

### 7.4 摘要回写 SQLite

必须说明：

1. 思源来源摘要是否回写到 `SQLite`
2. 是否标记 `source_type = siyuan`

实际填写：

1. 回写能力：`已支持。真实运行 run-siyuan-verify-3 后，SQLite knowledge_base 增加一条思源来源索引记录。`
2. `source_type`：`已标记为 siyuan。实查 data/storage/main.db 中存在 id = siyuan-note-1775117015911、source_type = siyuan 的记录。`

---

## 8. 去重与同步验收

### 8.1 去重能力

必须说明：

1. 是否按标题去重
2. 是否按来源路径去重
3. 是否避免重复导出

实际填写：

1. 标题去重：`已做基础控制。知识索引层按 workspace_id + knowledge_type + title + source 做同一身份判断。`
2. 来源去重：`已做。思源回写记录按 source 路径参与去重，避免同一导出路径被重复建索引。`
3. 重复导出控制：`当前只做到基础级别。重复执行不同 run_id 仍会生成不同导出文件，但同一路径的 SQLite 索引不会重复插入。`

### 8.2 同步策略

必须说明：

1. 当前是单向还是双向
2. 是否避免高频自动覆盖

实际填写：

1. 同步策略：`当前是单向同步。运行时把高价值知识导出到思源，再把摘要回写 SQLite；没有做思源到运行时的双向实时覆盖。`
2. 自动覆盖情况：`已避免高频自动覆盖。当前策略是新增导出文件，不覆盖已有文件内容。`

---

## 9. 前端与来源展示验收

必须说明：

1. 前端是否能展示 `siyuan` 来源
2. 是否不需要新增独立思源页面

实际填写：

1. 来源展示：`已支持最小展示。EventTimeline 与 LogsPanel 会读取 metadata.source_type 并展示 siyuan 标签。`
2. 页面情况：`未新增独立思源页面，仍沿用现有日志页和事件时间线展示来源。`

---

## 10. 手工测试记录

至少覆盖 5 类场景：

### 10.1 场景一：思源摘要搜索

1. 输入：`run-siyuan-search-1，请求 user_input 为“思源检索: 写入思源”。`
2. 预期：`命中 exported 目录中的思源 md 文件，返回路径与摘要，不返回全文。`
3. 实际结果：`通过。返回 2 条 exported 路径结果，摘要均为 “# 写入思源 / 写入思源”。`

### 10.2 场景二：思源正文读取

1. 输入：`run-siyuan-read-1，请求 user_input 为“读取思源: D:/newwork/本地智能体/data/siyuan/exported/main-run-siyuan-verify-3.md”。`
2. 预期：`读取指定 md 文件正文，并返回正文摘要。`
3. 实际结果：`通过。返回 “思源正文读取完成 … # 写入思源 / 写入思源”。`

### 10.3 场景三：知识导出到思源

1. 输入：`run-siyuan-verify-3，请求 user_input 为“写入思源”。`
2. 预期：`命中 write_siyuan_knowledge，生成导出文件，并在事件流中出现验证通过。`
3. 实际结果：`通过。run_finished 返回 “思源导出完成：D:/newwork/本地智能体/data/siyuan/exported/main-run-siyuan-verify-3.md”；导出文件已存在。`

### 10.4 场景四：思源摘要回写 SQLite

1. 输入：`在 run-siyuan-verify-3 完成后直接查询 data/storage/main.db。`
2. 预期：`knowledge_base 增加一条 source_type = siyuan 的记录，source 指向导出文件。`
3. 实际结果：`通过。SQLite 查询结果为 siyuan-note-1775117015911 / 写入思源 / D:/newwork/本地智能体/data/siyuan/exported\\main-run-siyuan-verify-3.md / siyuan。`

### 10.5 场景五：重复导出拦截

1. 输入：`先后执行 run-1775116598895-10 与 run-siyuan-verify-3，两次 user_input 都为“写入思源”。`
2. 预期：`不会覆盖旧文件；不同 run_id 允许形成不同导出文件；SQLite 索引至少能稳定保留一条思源记录。`
3. 实际结果：`通过。历史重复导出已清理；当前重复执行“写入思源”会直接复用 data/siyuan/exported/main-run-siyuan-reuse-5.md，不再新增导出文件，也不再新增重复 SQLite 索引。`

---

## 11. 构建与运行验证

实际填写：

1. `cargo build`：`通过。2026-04-02 执行 cargo build -p runtime-host，构建完成。`
2. `go build ./...`：`通过。2026-04-02 在 gateway/ 执行 go build ./...，无报错。`
3. `npm run build`：`通过。2026-04-02 在 frontend/ 执行 npm run build，构建完成。`

---

## 12. 偏离任务书的地方

实际填写：

1. `任务书建议新增独立 siyuan.rs / siyuan_search.rs / siyuan_writer.rs / siyuan_sync.rs，当前实现收敛在 execution.rs、memory_router.rs、knowledge.rs 中，没有再拆新模块。`
2. `实现仍然收敛在现有模块内，没有按任务书拆出独立思源模块，但能力边界与验收要求已经完成。`

---

## 13. 未完成项

### 13.1 阻断项

1. `无。当前没有阻断进入下一收口阶段的问题。`

### 13.2 非阻断项

1. `无。当前阶段要求的搜索、读取、导出、回写、来源展示与重复导出收口已全部完成。`
2. `无。后续能力增强不再计入本阶段未完成项。`

---

## 14. 验收结论

### 14.1 当前正式结论

1. 结论：`通过`
2. 验收日期：`2026-04-02`
3. 验收人：`Codex`

### 14.2 结论理由

1. `思源已正式进入运行时链路，搜索、读取、导出、SQLite 摘要回写和前端来源展示都有真实运行证据。`
2. `SQLite 与思源的主从边界是稳定的：SQLite 仍是主索引，思源只做外挂正文层与导出目录。`
3. `重复导出已真正收口为复用旧导出，历史重复文件与重复索引也已清理，本阶段不存在剩余尾项。`

---

## 15. 是否允许进入下一阶段

1. 结论：`允许`
2. 原因：`思源接入主链路已经跑通，正式功能点均有真实证据，遗留问题属于体验与收敛优化，不阻断下一阶段。`
