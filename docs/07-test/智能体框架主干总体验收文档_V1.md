# 本地智能体 - 智能体框架主干总体验收文档 V1

更新时间：2026-04-02

状态：`当前有效`

执行标记：

- `当前主干阶段总验收入口`
- `用于统一收口 M2 - M6 分段验收结果`
- `后续是否进入下一阶段，以本文结论为准`

关联文档：

- [智能体框架主干开发任务书_V1](D:/newwork/本地智能体/docs/06-development/智能体框架主干开发任务书_V1.md)
- [M2_上下文装配验收记录_V1](D:/newwork/本地智能体/docs/07-test/M2_上下文装配验收记录_V1.md)
- [M3_工具协议验收记录_V1](D:/newwork/本地智能体/docs/07-test/M3_工具协议验收记录_V1.md)
- [M4_压缩与Artifact验收记录_V1](D:/newwork/本地智能体/docs/07-test/M4_压缩与Artifact验收记录_V1.md)
- [M4_记忆系统验收记录_V1](D:/newwork/本地智能体/docs/07-test/M4_记忆系统验收记录_V1.md)
- [M5_验证链与完成判定验收记录_V1](D:/newwork/本地智能体/docs/07-test/M5_验证链与完成判定验收记录_V1.md)
- [M6_模型适配层验收记录_V1](D:/newwork/本地智能体/docs/07-test/M6_模型适配层验收记录_V1.md)
- [M6_项目问答输出收口验收记录_V1](D:/newwork/本地智能体/docs/07-test/M6_项目问答输出收口验收记录_V1.md)

---

## 1. 文档目的

本文件只回答 4 个问题：

1. 智能体框架主干是否已经整体成立
2. 各分段能力是否已经进入真实主链路
3. 当前是否允许进入下一阶段
4. 还剩哪些遗留项必须在下一阶段前被记录

本文件不是新的开发任务书。  
本文件是当前主干阶段的总验收口径。

---

## 2. 验收范围

本次总体验收只覆盖以下主干能力：

1. Query Engine / Agent 主循环
2. Context Envelope / Prompt 装配
3. Tool Protocol / Tool Registry
4. Permission / Risk Gate
5. Memory / Knowledge Router
6. Artifact / Compaction
7. Verify / Finish 判定
8. Model Adapter / 统一模型接入
9. Go <-> Rust <-> Frontend 合同回流

本次明确不验收：

1. 多智能体编排
2. 浏览器自动化主链路
3. 桌面自动化主链路
4. 高级 worker 并发调度
5. 复杂 fallback 策略

---

## 3. 总体通过条件

只有同时满足以下条件，才可判定主干阶段通过：

1. 主循环存在且稳定。
2. 上下文装配不再散落在零散逻辑中。
3. 工具调用不再是隐式分支，而有统一协议。
4. 高风险动作能进入统一确认路径。
5. 长输出能外置，不再直接压入主上下文。
6. 记忆进入主链路，但仍保持按需读写。
7. Verify 和 Finish 不只是阶段名，而有实际判定逻辑。
8. 模型接入不再散落 provider 细节。
9. 三端构建通过，且关键字段能稳定回流。

---

## 4. 验收结论页

### 4.1 结论类型

总体验收只允许输出以下 3 种结论：

1. `通过`
2. `有条件通过`
3. `不通过`

### 4.2 当前正式结论

当前结论：`通过`

验收日期：`2026-04-02`

验收人：`Codex（实现对话自验收）`

### 4.3 结论理由

当前已满足：

1. Query Engine、Tool Registry、Context Builder、Risk Gate、Memory Router、Artifact、Verify、Completion、Model Adapter 等主干模块均已落地，并进入真实主链路。
2. Rust、Go、前端构建已通过。
3. M2 - M6 分段验收文档已形成，且已补充 `M6_项目问答输出收口验收记录_V1`。
4. 最新正式链路 `run-1775094756157-2` 已完成真实 `chat/run` 验收，结果为 `completion_status = completed`、`verification_passed = true`。
5. `project_answer / context_answer` 的输出已完成收口，最终答复不再混入 `tool_call / XML / 标签式动作`。

当前仍保留的非阻断项：

1. Rust 侧仍存在少量 `dead_code` 与未完全消费字段 warning。
2. 部分 snapshot / 结构化字段目前以“主干可用”为主，离高频深度消费还有优化空间。

---

## 5. 分模块总体验收

### 5.1 Query Engine / 主循环

验收点：

1. 存在统一运行入口。
2. 存在 `Analyze -> Plan -> Execute -> Observe -> Verify -> Finish` 主线。
3. 失败、完成、等待确认都能被主线吸收。

验收结果：

- 结论：`通过`
- 依据：主循环相关模块已拆出，并已进入 `runtime-core` 主结构；最新正式链路已完成 `Analyze -> Plan -> Execute -> Observe -> Verify -> Finish` 全链路运行。

### 5.2 Context Envelope / Prompt 装配

验收点：

1. 存在统一上下文封装对象。
2. Prompt 组装存在固定分层。
3. planner / answer 侧能够消费统一装配结果。

验收结果：

- 结论：`通过`
- 对应分段：M2
- 依据：`context_builder.rs + prompt.rs + repo_context.rs` 已形成统一装配入口，planner 与 answer 分支已消费统一上下文。

### 5.3 Tool Protocol / Tool Registry

验收点：

1. 存在 `ToolRegistry`。
2. 存在统一 `ToolSpec / ToolCall / ToolCallResult` 结构。
3. 工具调用不再依赖零散隐式逻辑。

验收结果：

- 结论：`通过`
- 对应分段：M3
- 依据：已形成统一 `ToolDefinition / ToolCall / ToolCallResult` 协议，工具可见性与结果结构均已收口。

### 5.4 Permission / Risk Gate

验收点：

1. 高风险动作能生成确认请求。
2. 确认结果能回流并恢复执行。
3. 风险和确认事件能被三端消费。

验收结果：

- 结论：`通过`
- 依据：工作区首次访问、高风险删除、高风险命令均已进入统一确认路径，确认结果可经 Go 控制面回流并恢复执行，前端可稳定消费确认事件。

### 5.5 Memory / Knowledge Router

验收点：

1. 存在结构化记忆 schema。
2. recall 返回 digest，而非全量灌入。
3. planner / context 装配能够消费记忆摘要。

验收结果：

- 结论：`通过`
- 对应分段：M4
- 依据：记忆与知识已按 digest 方式进入主链路，planner 与 context 装配均可消费相关摘要。

### 5.6 Artifact / Compaction

验收点：

1. 长输出可外置为 artifact。
2. 会话压缩有统一入口。
3. 主链路只消费摘要与引用，而不是直接吞大文本。

验收结果：

- 结论：`通过`
- 对应分段：M4
- 依据：工具结果已支持 artifact 外置，事件与主链路只消费摘要、预览与 `artifact_path`。

### 5.7 Verify / Finish

验收点：

1. Verify 有实际验证逻辑。
2. Finish 有显式完成判定。
3. 最终结果状态不再只是随意字符串。

验收结果：

- 结论：`通过`
- 对应分段：M5
- 依据：`verify.rs + completion.rs` 已真实参与验证与完成判定，最终状态不再只是字符串拼接。

### 5.8 Model Adapter

验收点：

1. 存在统一模型适配入口。
2. provider 细节不再散落在运行时各处。
3. 三端合同已同步相关字段。

验收结果：

- 结论：`通过`
- 对应分段：M6
- 依据：统一模型适配入口已替代散落 provider 调用，中文 `chat/run` 已完成真实链路验收。

### 5.9 Output 收口

验收点：

1. 项目问答输出不再混入伪工具调用。
2. 最终答复有明确语言与格式约束。
3. 真实 `chat/run` 路径已重新验收。

验收结果：

- 结论：`通过`
- 对应分段：M6
- 依据：最新正式验收 `run-1775094756157-2` 的最终答复已为纯中文自然语言，未再出现伪工具调用或标签式动作。

---

## 6. 构建与基础验证

本次总体验收必须重复确认以下构建：

1. `cargo build`
2. `go build ./...`
3. `npm run build`

当前构建结论：

- `cargo build`：通过
- `go build ./...`：通过
- `npm run build`：通过

补充说明：

1. 当前 Rust 侧仍存在若干 `dead_code` / 未完全消费字段 warning。
2. 这些 warning 当前不阻断主干阶段通过，但已进入遗留项记录。

---

## 7. 当前遗留项

### 7.1 非阻断遗留项

当前可接受的非阻断项：

1. 少量结构体字段或 snapshot 类型暂未完全进入消费链路。
2. 部分新结构已建立，但仍偏“主干预留”而非“全链路高频消费”。
3. Rust 侧存在 warning，说明还有清理空间。
4. `project_answer` 的项目说明质量已达到可用与可控，但距离更贴合项目文档语义仍有优化空间。

### 7.2 阻断项判定标准

如出现以下情况，应直接改判为 `不通过`：

1. 主循环关键阶段未真实接入。
2. Tool Registry 只是声明存在，实际主链路未使用。
3. Verify / Completion 只存在类型定义，没有真实运行逻辑。
4. Model Adapter 没有真正替代散落 provider 调用。
5. Go / Rust / 前端 合同不同步，导致真实链路异常。

---

## 8. 是否允许进入下一阶段

当前判断：`允许进入下一阶段`

前提条件：

1. 下一阶段不应重新推翻主干设计。
2. 下一阶段应围绕“主干收口与增强”，而不是扩张到高级枝叶。
3. 下一阶段开始前，应把当前遗留项明确记录下来。

不建议直接进入的方向：

1. 多智能体
2. 复杂 worker 并发
3. 全量自动化能力扩张
4. 大规模前端再改版

建议进入的下一阶段方向：

1. 主链路收口与 warning 清理
2. 主干真实接入率提升
3. 关键工具链和上下文链的稳定性增强
4. 基于主干的下一轮能力规划

---

## 9. 最终判定模板

后续验收型 AI 或人工如需继续复验，可按以下模板追加新版本结论：

```
## 总体验收结论

- 结论：通过 / 有条件通过 / 不通过
- 日期：YYYY-MM-DD
- 验收人：xxx

## 通过理由
- xxx
- xxx

## 遗留项
- xxx
- xxx

## 是否允许进入下一阶段
- 是 / 否

## 下一阶段建议
- xxx
```

---

## 10. 总体验收结论

- 结论：`通过`
- 日期：`2026-04-02`
- 验收人：`Codex（实现对话自验收）`

## 11. 通过理由

1. 任务书要求的 7 个主干模块均已落地，并进入真实运行链路。
2. Query Engine、Tool Registry、Context Envelope、Risk Gate、Memory Router、Artifact、Verify、Model Adapter 均已有对应实现与分段验收记录。
3. 最新正式 `chat/run` 验收已通过，最终结果为 `completion_status = completed`、`verification_passed = true`。
4. 输出收口问题已解决，最终答复不再混入 `tool_call / XML / 标签式动作`。
5. 三端构建通过，关键字段已稳定回流 Go 与前端。

## 12. 遗留项

1. Rust 侧仍有少量 warning 需在后续阶段清理。
2. 部分 snapshot 与扩展字段已可用，但消费深度仍可继续提升。
3. 项目问答质量仍可继续贴近项目文档语义做增强。

## 13. 是否允许进入下一阶段

- `是`

## 14. 下一阶段建议

1. 先做主链路收口与 warning 清理。
2. 提升上下文、知识、记忆链路的命中质量与稳定性。
3. 在不推翻当前主干的前提下继续增强单智能体能力。

## 15. 一句话结论

> 智能体运行时主干已经正式成立，可以进入下一阶段；后续重点应放在主链路增强与质量提升，而不是扩展高级能力面。
