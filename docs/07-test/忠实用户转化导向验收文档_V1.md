# 本地智能体 - 忠实用户转化导向验收文档 V1

更新时间：2026-04-04

状态：`当前有效`

执行标记：

1. `忠实用户转化导向正式验收模板`
2. `实现完成后必须按本文逐项填写`
3. `是否达到长期用户可用门槛，以本文结论为准`
4. `后续实现型 AI 不得跳过本文的真实样本与构建验证`

关联文档：

1. [忠实用户转化导向开发任务书_V1](D:/newwork/本地智能体/docs/06-development/忠实用户转化导向开发任务书_V1.md)
2. [第二阶段短期可用能力验收文档_V1](D:/newwork/本地智能体/docs/07-test/第二阶段短期可用能力验收文档_V1.md)
3. [产品级总体验收文档_V1](D:/newwork/本地智能体/docs/07-test/产品级总体验收文档_V1.md)

---

## 1. 文档目的

本文只回答 4 个问题：

1. 当前系统是否已经从“可用”推进到“值得长期使用”。
2. 用户是否已经能感知连续性、知识沉淀、可信执行和项目推进价值。
3. 当前产品是否已经具备让主工作流逐步迁移进来的基础。
4. 还剩哪些不阻断长期使用的后续优化项。

---

## 2. 填写说明

1. 每一节都必须写。
2. 必须写清“做了什么、没做什么、怎么验证的”。
3. 必须写明文件落点。
4. 必须附真实样本留证。
5. 每个工作包都必须给出 `通过 / 有条件通过 / 不通过`。
6. 整体结论只允许 `通过 / 有条件通过 / 不通过`。
7. 任何没有真实样本留证的小节，默认判定为 `不通过`。
8. 任何没有构建结果的小节，不得写“正式通过”。

---

## 3. 本轮目标

填写要求：

1. 用 1 到 3 句话写清本轮验收目标。
2. 明确本轮只验收什么，不验收什么。

实际填写：

1. 本轮只验收 `工作包 A：连续性与记忆产品化`，重点验证正式记忆查看、正式记忆删除，以及“上次做到哪 / 还差什么 / 下一步做什么”续推回答。
2. 本轮不验收工作包 B、C、D、F，工作包 E 只记录已完成的首页和设置页入口收口，不作为整包正式通过依据。

---

## 4. 实现范围

填写要求：

1. 只列出本轮实际完成的能力点。
2. 不要写计划项，只写已实现项。

实际填写：

1. 新增正式长期记忆列表接口，返回类型、来源、理由、作用范围、更新时间等真实字段。
2. 新增正式长期记忆删除能力，删除后同步影响 SQLite 实际存储与后续召回屏蔽。
3. 设置页新增正式记忆入口；首页新增“继续上次任务”与“下一步建议”入口；续推类问题优先命中会话状态回答。

---

## 5. 修改文件清单

填写要求：

1. 只列出本轮实际修改或新增的文件。
2. 路径必须具体。

实际填写：

1. `crates/runtime-core/src/paths.rs`
2. `crates/runtime-core/src/memory.rs`
3. `crates/runtime-core/src/planner.rs`
4. `crates/runtime-core/src/execution.rs`
5. `gateway/go.mod`
6. `gateway/go.sum`
7. `gateway/internal/api/router.go`
8. `gateway/internal/contracts/contracts.go`
9. `gateway/internal/memory/store.go`
10. `gateway/internal/memory/store_test.go`
11. `frontend/src/App.tsx`
12. `frontend/src/shared/contracts.ts`
13. `frontend/src/settings/api.ts`
14. `frontend/src/settings/useSettings.ts`
15. `frontend/src/settings/SettingsPanel.tsx`
16. `frontend/src/workspace/WorkbenchOverview.tsx`
17. `frontend/src/index.css`

---

## 6. 总体验收结论页

### 6.1 结论类型

总体验收只允许以下 3 种结论：

1. `通过`
2. `有条件通过`
3. `不通过`

### 6.2 当前正式结论

1. 结论：`有条件通过`
2. 验收日期：`2026-04-04`
3. 验收人：`Codex`

### 6.3 结论理由

填写要求：

1. 用 3 到 6 条写清结论依据。
2. 结论依据必须是已实现和已验证的事实。

实际填写：

1. 工作包 A 已形成真实记忆查看、删除、续推三条主链路，并生成了对应留证样本。
2. `cargo build`、`go build ./...`、`npm run build` 本轮均已通过。
3. 最新续推样本中 `tool_name=session_context`，说明“上次做到哪 / 还差什么 / 下一步做什么”已经实际命中新续推逻辑。

---

## 7. 工作包 A 验收：连续性与记忆产品化

### 7.1 用户是否能看见系统记住了什么

必须说明：

1. 是否存在正式记忆查看入口。
2. 记忆是否区分类型。
3. 展示是否使用真实数据。

实际填写：

1. 存在正式记忆查看入口，位于设置页 `Memory Policy` 模块下的“正式记忆入口”列表。
2. 记忆已区分 `kind` 类型，真实样本可见 `project_rule`、`workflow_preference`、`lesson_learned` 等类型。
3. 展示使用真实接口数据，留证见 `tmp/loyal-user-acceptance/memory-visibility-sample.json`。

### 7.2 用户是否能理解为什么记住

必须说明：

1. 是否能看到来源。
2. 是否能看到理由。
3. 是否能看到更新时间或作用范围。

实际填写：

1. 记忆条目可看到 `source` 来源字段。
2. 记忆条目可看到 `reason` 理由字段。
3. 记忆条目可看到 `updated_at` 更新时间与 `scope` 作用范围，留证见 `tmp/loyal-user-acceptance/memory-visibility-sample.json`。

### 7.3 会话续推和项目续推是否更稳定

必须说明：

1. “继续推进”“上次做到哪”“还差什么”是否更稳定。
2. 是否存在真实项目续推样本。
3. 失败时是否有受控回退。

实际填写：

1. “继续推进”“上次做到哪”“还差什么”已优先命中会话续推逻辑，回答直接给出当前计划、阻塞与下一步建议。
2. 存在真实续推样本，留证见 `tmp/loyal-user-acceptance/project-continue-sample.json`。
3. 原有项目问答恢复路径仍保留，续推增强没有移除受控回退。

### 7.4 本工作包结论

1. 结论：`通过`
2. 结论理由：`正式记忆查看、删除、续推三项均已落地并有真实样本，且三端构建通过。`

---

## 8. 工作包 B 验收：知识沉淀与治理增强

### 8.1 知识是否已区分来源与可信度

必须说明：

1. 是否能区分来源类型。
2. 是否能体现可信度或 verified 口径。
3. 是否能看到更新时间。

实际填写：

1. 知识检索结果已经区分 `文档知识 / 运行时沉淀知识 / 用户确认知识` 三类来源，真实留证见 `tmp/loyal-user-acceptance/knowledge-recall-sample.json`。
2. 检索结果已展示可信度口径，包含“高（直接文件命中）”“高（已验证沉淀）”“高（用户沉淀确认）”等说明。
3. 检索结果已能看到 `updated_at` 更新时间；文档型来源暂无时间时会明确显示“未提供”。 

### 8.2 高价值知识是否自动沉淀

必须说明：

1. 是否存在任务完成后的自动沉淀。
2. 沉淀内容是否真的可复用。
3. 是否有真实样本留证。

实际填写：

1. 已存在任务完成后的自动沉淀，当前覆盖 `knowledge_search` 成功结果写入 SQLite 与 JSONL，并同步到思源导出。
2. 自动沉淀内容为可复用知识摘要，而不是单纯过程日志，真实样本中可见 `record_type=knowledge_recall`。
3. 真实样本留证见 `tmp/loyal-user-acceptance/knowledge-auto-write-sample.json`。

### 8.3 低价值知识是否继续被拦截

必须说明：

1. 是否继续存在低价值知识跳过。
2. 是否能看见跳过原因。
3. 是否有真实样本留证。

实际填写：

1. 低价值知识拦截仍保持成立，当前已覆盖摘要过短、未验证通过、项目说明回显等场景。
2. 跳过原因已经单独留到 `knowledge_write_skipped` 事件中，可直接看到具体原因文本。
3. 真实样本留证见 `tmp/loyal-user-acceptance/knowledge-skip-sample.json`。

### 8.4 本工作包结论

1. 结论：`通过`
2. 结论理由：`知识来源与可信度、自动沉淀、低价值拦截三项均已形成最小正式闭环，并有真实样本留证。`

---

## 9. 工作包 C 验收：可信执行与验证收口

### 9.1 执行结果是否更可信

必须说明：

1. 执行前意图是否可见。
2. 执行后验证是否可见。
3. 成功与失败是否能明确区分。

实际填写：

1. 执行前意图已可见，真实样本中 `plan_ready / action_requested` 都能看到任务标题、工具名和下一步信息。
2. 执行后验证已可见，`verification_summary` 现已同时包含结果摘要和“执行依据”说明。
3. 成功与失败已能明确区分，成功样本见 `tmp/loyal-user-acceptance/trusted-execution-success.json`，失败样本见 `tmp/loyal-user-acceptance/trusted-execution-failure.json`。

### 9.2 失败后是否更容易继续推进

必须说明：

1. 是否有“继续建议”。
2. 是否有真实失败样本。
3. 是否能避免流程直接断掉。

实际填写：

1. 已补“继续建议”，失败事件中 `metadata.next_step` 与 `failure_recovery_hint` 会直接给出下一步动作。
2. 已存在真实失败样本，留证见 `tmp/loyal-user-acceptance/trusted-execution-failure.json`。
3. 当前失败不会直接断掉，真实样本中可见按工具类型给出的继续建议；受控恢复样本见 `tmp/loyal-user-acceptance/recovery-path-sample.json`。

### 9.3 风险确认与恢复解释是否更清楚

必须说明：

1. 风险确认卡是否信息完整。
2. 恢复路径是否可见。
3. 是否有真实样本留证。

实际填写：

1. 风险确认卡信息已完整，真实样本中可见 `action_summary / reason / impact_scope / target_paths / hazards / alternatives / reversible`。
2. 恢复路径已可见，真实样本中可见 `verified_with_recovery` 口径和恢复后的结果收口。
3. 真实样本留证见 `tmp/loyal-user-acceptance/trusted-risk-confirmation.json` 与 `tmp/loyal-user-acceptance/recovery-path-sample.json`。

### 9.4 本工作包结论

1. 结论：`通过`
2. 结论理由：`执行前意图、执行后验证、失败后继续建议、风险确认与恢复留痕已经形成最小正式闭环，并有真实样本留证。`

---

## 10. 工作包 D 验收：项目推进与状态理解增强

### 10.1 项目状态类回答是否成立

必须说明：

1. 是否稳定回答“已完成能力 / 当前阶段 / 待收口项”。
2. 是否稳定命中核心文档。
3. 是否存在真实样本留证。

实际填写：

1. 项目状态类回答已稳定输出“已完成能力 / 当前阶段 / 待收口项”，并在真实样本中展开到验证路径与完成标准。
2. 项目状态回答已优先命中 `docs/06-development/忠实用户转化导向开发任务书_V1.md` 与 `docs/07-test/忠实用户转化导向验收文档_V1.md`。
3. 已提供该工作包独立样本：`tmp/loyal-user-acceptance/project-status-loyal-summary.json` 与 `tmp/loyal-user-acceptance/next-step-sample.json`。

### 10.2 回答是否能说明“做到什么程度、为什么这样判断、下一步做什么”

必须说明：

1. 是否说明做到什么程度。
2. 是否说明为什么这样判断。
3. 是否说明下一步该做什么。

实际填写：

1. “这个项目现在做到什么程度了”已能直接回答当前已完成能力、当前阶段和待收口项，留证见 `tmp/loyal-user-acceptance/project-status-loyal-summary.json`。
2. 回答已明确说明判断依据来自忠实用户转化导向开发任务书、验收文档和既有真实样本留证。
3. “现在最该做什么，继续下一步”已形成独立项目状态样本，留证见 `tmp/loyal-user-acceptance/next-step-sample.json`。

### 10.3 项目状态类回答是否和真实留证绑定

必须说明：

1. 是否说明相关能力已有真实样本留证。
2. 是否能展开到样本、验证路径或完成标准。
3. 缓存闭环是否仍成立。

实际填写：

1. 工作包 D 已与独立项目状态样本正式绑定，样本文件为 `tmp/loyal-user-acceptance/project-status-loyal-summary.json` 与 `tmp/loyal-user-acceptance/next-step-sample.json`。
2. 回答已展开到样本路径、验收文档路径和“回答需明确指出已完成能力、当前阶段、待收口项，并进一步指向样本或完成标准”的完成标准。
3. 缓存闭环仍保持成立，本轮真实样本可见 `project_answer` 的 `cache_status=miss` 与后续回写路径。

### 10.4 本工作包结论

1. 结论：`通过`
2. 结论理由：`工作包 D 已完成最小正式收口，项目状态回答与真实样本、验证路径、完成标准已绑定。`

---

## 11. 工作包 E 验收：日常使用闭环与前端工作台收口

### 11.1 首页是否更像日常工作台

必须说明：

1. 首页是否突出当前任务。
2. 首页是否突出当前阶段。
3. 首页是否突出下一步建议。

实际填写：

1. 首页已新增“继续上次任务”按钮，突出当前任务继续推进。
2. 首页保留当前状态与工作区信号。
3. 首页已新增“下一步建议”卡片。

### 11.2 日志页是否更像证据与复盘页

必须说明：

1. 验证与证据链是否更突出。
2. 记忆、知识、缓存、失败原因是否更容易看见。
3. 是否有真实页面留证。

实际填写：

1. 本轮已专项调整日志页，把“当前结论、执行依据、验证结果、缓存状态、记忆与知识、下一步”前置到首屏复盘摘要区，证据链不再只靠下方技术明细拼出来。
2. 记忆、知识、缓存、失败原因和验证后续都已在日志页首屏或右侧证据链明细中直接可见，不需要继续翻多层详情。
3. 已新增日志页真实留证：`tmp/loyal-user-acceptance/logs-review-sample.json`。

### 11.3 用户是否更容易知道下一步该做什么

必须说明：

1. 是否存在“继续上次任务”或类似入口。
2. 是否存在“查看当前长期规则 / 最近沉淀”之类入口。
3. 是否有真实使用样本。

实际填写：

1. 首页存在“继续上次任务”入口。
2. 设置页存在“正式记忆入口”。
3. 真实使用样本见 `tmp/loyal-user-acceptance/workspace-dashboard-sample.json` 与 `tmp/loyal-user-acceptance/project-continue-sample.json`。

### 11.4 本工作包结论

1. 结论：`通过`
2. 结论理由：`首页、设置页与日志页已形成最小工作台闭环，日志页复盘体验已补到首屏证据链与下一步建议，并有真实样本留证。`

---

## 12. 工作包 F 验收：外部连接优先级预留

### 12.1 是否已形成清晰优先级

必须说明：

1. 是否已固定优先级顺序。
2. 是否已写入正式文档。
3. 是否存在接口位置预留。

实际填写：

1. 开发文档中已固定优先级顺序。
2. 优先级已写入正式文档。
3. 已补接口位置预留验收，当前预留落点位于运行时工具注册层与设置接口返回结构，并在设置页提供可见入口；留证见 `tmp/loyal-user-acceptance/external-connection-priority-sample.json`。

### 12.2 是否仍保持“预留、不失控扩展”的边界

必须说明：

1. 是否没有提前做重型连接器。
2. 是否只做了文档和接口层预留。
3. 是否保留下一阶段继续推进空间。

实际填写：

1. 本轮没有提前实现重型连接器。
2. 本轮只补了文档、运行时工具注册层、设置接口与设置页展示的预留信息，没有引入任何重型外部连接器。
3. 下一阶段空间仍然保留。

### 12.3 本工作包结论

1. 结论：`通过`
2. 结论理由：`外部连接优先级已固定，运行时工具注册层与设置接口已补正式预留位置，并新增真实样本完成核对，且没有失控扩展到重型连接器。`

---

## 13. 构建与运行验证

必须填写以下验证结果：

1. `cargo build`
2. `go build ./...`
3. `npm run build`
4. 至少一轮项目状态样本
5. 至少一轮会话续推样本
6. 至少一轮执行成功或失败样本
7. 至少一轮记忆或知识沉淀样本

实际填写：

1. `cargo build`：`已通过`
2. `go build ./...`：`已通过`
3. `npm run build`：`已通过`
4. 真实项目状态样本：`tmp/loyal-user-acceptance/project-status-loyal-summary.json`、`tmp/loyal-user-acceptance/next-step-sample.json`
5. 会话续推样本：`tmp/loyal-user-acceptance/project-continue-sample.json`
6. 文件读写或命令执行样本：`tmp/loyal-user-acceptance/trusted-execution-success.json`、`tmp/loyal-user-acceptance/trusted-execution-failure.json`、`tmp/loyal-user-acceptance/trusted-risk-confirmation.json`、`tmp/loyal-user-acceptance/recovery-path-sample.json`
7. 记忆或知识沉淀样本：`tmp/loyal-user-acceptance/memory-visibility-sample.json`、`tmp/loyal-user-acceptance/memory-delete-sample.json`、`tmp/loyal-user-acceptance/knowledge-recall-sample.json`、`tmp/loyal-user-acceptance/knowledge-auto-write-sample.json`、`tmp/loyal-user-acceptance/knowledge-skip-sample.json`、`tmp/loyal-user-acceptance/logs-review-sample.json`、`tmp/loyal-user-acceptance/external-connection-priority-sample.json`

---

## 14. 真实样本清单

本阶段默认样本目录：

`tmp/loyal-user-acceptance/`

至少应存在以下样本：

1. `memory-visibility-sample.json`
2. `memory-delete-sample.json`
3. `project-continue-sample.json`
4. `knowledge-recall-sample.json`
5. `knowledge-auto-write-sample.json`
6. `knowledge-skip-sample.json`
7. `trusted-execution-success.json`
8. `trusted-execution-failure.json`
9. `trusted-risk-confirmation.json`
10. `project-status-loyal-summary.json`
11. `next-step-sample.json`
12. `workspace-dashboard-sample.json`
13. `recovery-path-sample.json`
14. `logs-review-sample.json`
15. `external-connection-priority-sample.json`

如果本轮只推进单个工作包，可以只补该工作包直接相关样本，但样本命名必须稳定。

---

## 15. 当前未完成项

### 15.1 阻断项

填写要求：

1. 如果没有，明确写 `无`。
2. 如果有，必须写清是否阻断长期用户可用门槛。

实际填写：

1. 无

### 15.2 非阻断项

填写要求：

1. 只写当前真实还存在但不阻断结论的问题。
2. 每条要简明说明影响范围。

实际填写：

1. 工作包 E 已达通过门槛，但日志页当前真实留证仍以 JSON 结构样本为主，后续可再补页面级截图或录屏留证以方便非研发验收。

---

## 16. 最终验收结论

填写要求：

1. 必须明确写 `通过 / 有条件通过 / 不通过`。
2. 如果是 `有条件通过`，必须写清哪些问题不阻断长期使用。
3. 如果是 `不通过`，必须写清阻断点是什么。

实际填写：

1. 结论：`通过`
2. 结论理由：`工作包 A、B、C、D、E、F 均已形成真实留证和构建验证，当前系统已达到忠实用户转化导向的正式通过门槛。`
