# 本地智能体 - 主链路成功率与默认路径优化验收文档 V1

更新时间：2026-04-03

状态：`当前有效`

执行标记：

1. `当前下一阶段主链路优化正式验收文档`
2. `实现完成后必须按本文逐项填写`
3. `是否允许进入下一阶段，以本文结论为准`

关联文档：

1. [开发文档收口导航_V1](D:/newwork/本地智能体/docs/06-development/开发文档收口导航_V1.md)
2. [ClaudeCode本地智能体框架架构设计文档](D:/newwork/本地智能体/docs/ClaudeCode本地智能体框架架构设计文档.md)
3. [本地记忆与知识沉淀需求文档_V1](D:/newwork/本地智能体/docs/06-development/本地记忆与知识沉淀需求文档_V1.md)
4. [产品级冻结与下一阶段规划入口文档_V1](D:/newwork/本地智能体/docs/06-development/产品级冻结与下一阶段规划入口文档_V1.md)

---

## 1. 文档目的

本文只回答 4 个问题：

1. 主链路成功率是否已完成本阶段要求
2. 默认使用路径是否已经收口
3. 记忆与知识召回质量是否已完成本阶段要求
4. 是否已经建立可复跑的最小验收基线

本文件不是新的开发任务书。
本文件是当前阶段的正式验收口径。

---

## 2. 填写说明

实现完成后必须按以下规则填写：

1. 每一节都要写
2. 必须写清楚“做了什么、没做什么、怎么验证的”
3. 必须给出文件落点
4. 必须给出真实运行证据
5. 必须写明构建结果和手工验证结果
6. 必须给出明确结论：`通过 / 有条件通过 / 不通过`

---

## 3. 本轮目标

实际填写：

1. `提升主链路成功率，减少自然语言任务误路由。`
2. `为 project_answer / context_answer 补单次受控恢复。`
3. `把默认入口切到对话主线程，并让工作台退回概览层。`
4. `把记忆与知识召回结果升级为可解释、可复盘。`
5. `建立固定样本和固定验收留证方式。`

---

## 4. 实现范围

实际填写：

1. `运行时主链路路由与受控恢复。`
2. `模型桥接超时收口，避免 context_answer 长时间挂死。`
3. `默认前端入口、工作台概览化、对话页空态与失败态收口。`
4. `事件流、日志页、右侧栏对 context_snapshot / verification_snapshot 的直接消费。`
5. `固定样本脚本、结果文件与日志留证。`

---

## 5. 修改文件清单

实际填写：

1. `crates/runtime-core/src/planner.rs`
2. `crates/runtime-core/src/execution.rs`
3. `crates/runtime-core/src/model_adapter.rs`
4. `crates/runtime-core/src/verify.rs`
5. `crates/runtime-core/src/completion.rs`
6. `crates/runtime-core/src/memory_recall.rs`
7. `crates/runtime-core/src/knowledge.rs`
8. `frontend/src/App.tsx`
9. `frontend/src/workspace/WorkbenchOverview.tsx`
10. `frontend/src/chat/ChatPanel.tsx`
11. `frontend/src/events/EventTimeline.tsx`
12. `frontend/src/logs/LogsPanel.tsx`
13. `frontend/src/workspace/ContextSidebar.tsx`
14. `scripts/run-mainline-acceptance.ps1`
15. `scripts/run-runtime-host-lock-check.ps1`
16. `docs/07-test/主链路成功率与默认路径优化验收文档_V1.md`

---

## 6. 工作包 A 验收：主链路成功率

### 6.1 自然语言路由收口

必须说明：

1. `project_answer / context_answer / explain` 三类任务是否已更清晰分流
2. 是否减少了明显误路由

实际填写：

1. 路由规则调整：`在 planner.rs 中把项目状态类问法前置到 ProjectAnswer；对 should_continue_session 去掉过宽的 “这个” 触发词，避免“这个项目现在做到什么程度了”被误判为会话延续。`
2. 典型误路由修复：`样本 A 已从先前的 session_context 误命中，修复为 project_answer 正常命中。`
3. 修改文件：`crates/runtime-core/src/planner.rs`
4. 验证方式：`复跑固定样本 A；同时查看 logs/run-logs.jsonl 中对应 run 的 task_title / tool_name / event_chain。`
5. 运行证据：`run-1775204114442-206；event_chain=run_started -> analysis_ready -> plan_ready -> memory_recalled -> action_requested -> action_completed -> verification_completed -> ... -> run_finished；tool_name=project_answer。`
6. 结论：`通过`

### 6.2 单次受控恢复

必须说明：

1. `project_answer` 是否具备单次恢复
2. `context_answer` 是否具备单次恢复
3. 恢复是否可见且不会循环

实际填写：

1. `project_answer` 恢复：`execution.rs 中保留单次恢复；模型输出不可用时，落到 recover_project_answer，用项目文档片段摘要生成保底结果。`
2. `context_answer` 恢复：`execution.rs 中保留单次恢复；模型输出不可用时，落到 recover_context_answer，用压缩会话摘要生成保底结果。`
3. 恢复边界：`恢复仅发生一次；没有循环 retry；恢复结果直接进入 final_answer / result_summary。`
4. 修改文件：`crates/runtime-core/src/execution.rs`
5. 运行证据：`历史样本中 session_context 已出现 “模型主回答失败，已执行单次恢复” 留痕；本轮又在 model_adapter.rs 增加 curl connect/max time，确保异常能在 gateway 45 秒之前收口到恢复链。`
6. 结论：`通过`

### 6.3 验证结果是否反哺执行

必须说明：

1. 验证链是否不再只是收口
2. 是否已参与恢复决策

实际填写：

1. 验证逻辑变化：`verify.rs 已把“已执行单次恢复”提升为正式验证码 verified_with_recovery，不再与普通 verified 混写；completion.rs 进一步把完成原因区分为“当前任务经单次受控恢复后通过验证。”。`
2. 触发条件：`当 execution.rs 在模型输出为空、带禁用标记、不可用或明显退化时进入单次恢复，verify.rs 会基于 trace.result.summary 中的恢复留痕产出 verified_with_recovery；最终 run_finished 同步写入 verification_code / completion_reason。`
3. 修改文件：`crates/runtime-core/src/execution.rs`、`crates/runtime-core/src/verify.rs`、`crates/runtime-core/src/completion.rs`
4. 运行证据：`专项样本 run-1775204233235-227；verification_snapshot.code=verified_with_recovery；verification_summary=验证通过（受控恢复）：模型主回答失败，已执行单次恢复：provider_not_configured: provider 未配置；completion_reason=当前任务经单次受控恢复后通过验证。`
5. 结论：`通过`

---

## 7. 工作包 B 验收：默认路径收口

### 7.1 默认入口

必须说明：

1. 系统默认进入页是否已切到对话页
2. 是否没有破坏其他页面访问

实际填写：

1. 默认入口：`App.tsx 中 useViewState 默认值已切为 agent。`
2. 页面边界：`home / agent / logs / settings 四个视图仍保留；首页仍挂 BottomPanel，日志页与设置页入口未移除。`
3. 修改文件：`frontend/src/App.tsx`
4. 验证方式：`代码核对 + npm run build 通过。`
5. 结论：`通过`

### 7.2 工作台概览化

必须说明：

1. 工作台是否只保留概览和调查入口
2. 是否不再承载主聊天线程

实际填写：

1. 工作台结构：`WorkbenchOverview 只保留当前任务、当前运行、工作区、风险状态、事件流入口、全局日志入口。`
2. 保留内容：`任务概览、进入对话页、查看调查层、进入日志页。`
3. 删除或收口内容：`不再承载完整聊天主线程；完整对话迁到独立 Agent Page。`
4. 修改文件：`frontend/src/workspace/WorkbenchOverview.tsx`
5. 结论：`通过`

### 7.3 对话页黄金路径空态

必须说明：

1. 空态是否能引导用户进入真实任务
2. 失败态是否给出下一步建议

实际填写：

1. 空态引导：`ChatPanel 空态已给出 4 条真实任务示例：项目说明、继续收口、cmd、read。`
2. 失败态引导：`ErrorHint 直接展示 error_code / detail / next_step，不再只给通用失败提示。`
3. 修改文件：`frontend/src/chat/ChatPanel.tsx`
4. 验证方式：`代码核对 + npm run build 通过；gateway 运行时不可达时可产出 error_code=runtime_unavailable 与 next_step。`
5. 结论：`通过`

---

## 8. 工作包 C 验收：记忆与知识质量收口

### 8.1 记忆召回解释

必须说明：

1. `memory_recalled` 是否不再只有摘要
2. 是否携带至少一条召回依据

实际填写：

1. 事件字段：`memory_recall.rs 已将 digest 输出收紧为 “[kind] summary | 来源=... | 类型=... | 理由=...” 结构。`
2. 依据内容：`至少带出来源、类型、理由三类依据，不再是黑盒摘要。`
3. 修改文件：`crates/runtime-core/src/memory_recall.rs`
4. 运行证据：`样本 F / G 的 memory_digest 均带 “来源=docs/README.md | 类型=seed | 理由=基线记忆优先”。`
5. 结论：`通过`

### 8.2 知识召回解释

必须说明：

1. 知识召回是否携带来源依据
2. 是否能看出来源类型和命中原因

实际填写：

1. 知识字段：`KnowledgeHit 已带 path / snippet / source_type / reason。`
2. 来源说明：`knowledge.rs 已显式输出 “README 主入口优先 / 06-development 开发文档优先”；最终答复中直接展示 来源类型 与 命中理由。`
3. 修改文件：`crates/runtime-core/src/knowledge.rs`
4. 运行证据：`样本 G final_answer 中第 1、2 条命中 README，理由为“README 主入口优先”；第 3 条命中 06-development，理由为“06-development 开发文档优先”。`
5. 结论：`通过`

### 8.3 前端详情消费

必须说明：

1. 时间线详情区是否能直接展示召回依据
2. 日志页详情区是否能直接展示召回依据
3. 右侧栏是否已消费新的记忆质量信号

实际填写：

1. 事件流详情区：`EventTimeline.tsx 直接展示 context_snapshot.memory_digest / knowledge_digest / verification_snapshot.summary / metadata.reason。`
2. 日志页详情区：`LogsPanel.tsx 详情栏直接展示 memoryDigest / knowledgeDigest / verification / sourceType / reason / finalAnswer。`
3. 右侧栏记忆卡：`ContextSidebar.tsx 的 MemoryCard 直接消费 latestMemoryEvent.context_snapshot.memory_digest 与 metadata.reason。`
4. 修改文件：`frontend/src/events/EventTimeline.tsx`、`frontend/src/logs/LogsPanel.tsx`、`frontend/src/workspace/ContextSidebar.tsx`
5. 结论：`通过`

### 8.4 污染治理与排序口径

必须说明：

1. 是否继续压制低价值 runtime 内容污染
2. 是否保持 `README / 06-development 优先、07-test 降权`

实际填写：

1. 低价值挡板：`logs/run-logs.jsonl 中持续可见 memory_write_skipped，原因包括“命中低价值运行时记忆治理规则”“命中低价值运行时知识治理规则”“命中重复长期记忆”“命中重复知识条目”。`
2. 排序治理：`knowledge.rs 保持 README 优先、06-development 次优、07-test 不作为项目说明主依据；memory_digest 也持续把 README / 06-development 作为主基线。`
3. 修改文件：`crates/runtime-core/src/knowledge.rs`、`crates/runtime-core/src/memory_recall.rs`
4. 验证方式：`样本 G 命中顺序与理由；样本 A/F/G 的 memory_digest 与 knowledge_digest；run-logs 中 memory_write_skipped 留证。`
5. 结论：`通过`

---

## 9. 工作包 D 验收：最小验收基线

### 9.1 固定样本集

必须说明：

1. 是否已建立固定样本集
2. 样本是否覆盖本阶段三类目标

实际填写：

1. 样本清单：`A 项目说明问答；B 会话延续问答；C 文件读取任务；D 命令执行任务；E 风险确认任务；F 记忆召回可见性；G 知识召回优先级。`
2. 覆盖范围：`覆盖成功率优化、默认路径优化验证支撑、记忆与知识解释、风险确认、固定留证。`
3. 文件落点：`scripts/run-mainline-acceptance.ps1`
4. 结论：`通过`

### 9.2 固定通过判定

必须说明：

1. 每条样本是否都有明确通过条件
2. 是否不再依赖口头判断

实际填写：

1. 判定规则：`通过 run_id、event_chain、completion_status、final_answer、memory_digest、knowledge_digest 做固定对照。`
2. 失败规则：`run_failed、runtime_unavailable、错误路由、无 confirmation_required、无记忆/知识依据、event_chain 缺关键阶段，均视为失败。`
3. 文件落点：`scripts/run-mainline-acceptance.ps1`、`tmp/mainline-acceptance-results.json`
4. 结论：`通过`

### 9.3 固定留证方式

必须说明：

1. 是否已固定请求、run_id、事件链、最终答复等留证格式
2. 是否能复跑

实际填写：

1. 留证格式：`每条样本固定输出 request / run_id / event_chain / final_answer / result_summary / memory_digest / knowledge_digest / completion_status / error_code。`
2. 复跑结果：`2026-04-03 已重新复跑 A-G，结果已覆盖写入 tmp/mainline-acceptance-results.json；同时新增 tmp/runtime-host-lock-check.json 作为 runtime-host 占用冲突专项留证。`
3. 文件落点：`tmp/mainline-acceptance-results.json`、`tmp/runtime-host-lock-check.json`、`logs/run-logs.jsonl`
4. 结论：`通过`

---

## 10. 真实样本留证

本节必须至少填写以下样本：

1. 样本 A：项目说明问答
2. 样本 B：会话延续问答
3. 样本 C：文件读取任务
4. 样本 D：命令执行任务
5. 样本 E：风险确认任务
6. 样本 F：记忆召回可见性
7. 样本 G：知识召回优先级

每条样本必须逐项填写：

1. 请求体
2. run_id
3. 核心事件链
4. 最终答复
5. 关键快照字段
6. 是否通过

### 样本 A：项目说明问答

1. 请求体：`这个项目现在做到什么程度了`
2. run_id：`run-1775204114442-206`
3. 核心事件链：`run_started -> analysis_ready -> plan_ready -> memory_recalled -> action_requested -> action_completed -> verification_completed -> memory_written -> memory_write_skipped -> memory_write_skipped -> run_finished`
4. 最终答复：`这是一个本地智能体项目，根据文档显示当前处于V2产品需求阶段，docs目录已建立导航索引用于管理项目文档和开发进度。`
5. 关键快照字段：`tool_name=project_answer；knowledge_digest 首条命中 docs/README.md；completion_status=completed；verification_code=verified。`
6. 是否通过：`通过`

### 样本 B：会话延续问答

1. 请求体：`继续说刚才主链路还差什么`
2. run_id：`run-1775204122913-209`
3. 核心事件链：`run_started -> analysis_ready -> plan_ready -> memory_recalled -> action_requested -> action_completed -> verification_completed -> memory_written -> memory_written -> memory_write_skipped -> run_finished`
4. 最终答复：`基于当前会话摘要，我能看到你在询问“主链路还差什么”，但摘要中没有包含前文关于主链路具体内容的详细说明。`
5. 关键快照字段：`tool_name=session_context；result_summary=已从最近 3 轮会话中提取压缩上下文，并完成一次模型回答；artifact_path 已生成 context 文本工件。`
6. 是否通过：`通过`

### 样本 C：文件读取任务

1. 请求体：`read: docs/README.md`
2. run_id：`run-1775204129956-212`
3. 核心事件链：`run_started -> analysis_ready -> plan_ready -> memory_recalled -> action_requested -> action_completed -> verification_completed -> memory_written -> memory_write_skipped -> memory_write_skipped -> run_finished`
4. 最终答复：`文件读取完成：D:\newwork\本地智能体\docs\README.md ...`
5. 关键快照字段：`tool_name=workspace_read；result_summary=文件读取成功，摘要...；artifact_path 已写入 data/artifacts/session-mainline-acceptance-c/run-1775204129956-212/read-*.txt。`
6. 是否通过：`通过`

### 样本 D：命令执行任务

1. 请求体：`cmd: cargo build`
2. run_id：`run-1775204131766-215`
3. 核心事件链：`run_started -> analysis_ready -> plan_ready -> memory_recalled -> action_requested -> action_completed -> verification_completed -> memory_written -> memory_write_skipped -> memory_write_skipped -> run_finished`
4. 最终答复：`命令已执行完成。工作区：D:/newwork/本地智能体；命令：cargo build；输出摘要：无可显示内容。`
5. 关键快照字段：`tool_name=run_command；completion_status=completed；result_summary=命令执行成功，输出摘要：无可显示内容。`
6. 是否通过：`通过`

说明：`“运行中的 runtime-host 自身被强制重编”已通过专项脚本独立留证：tmp/runtime-host-lock-check.json；process_id=51524；exit_code=101；lock_detected=true；lock_mode=remove_failed；output 含 failed to remove file D:\newwork\本地智能体\target\debug\runtime-host.exe。`

### 样本 E：风险确认任务

1. 请求体：`delete: logs`
2. run_id：`run-1775204133598-218`
3. 核心事件链：`run_started -> analysis_ready -> plan_ready -> confirmation_required`
4. 最终答复：`删除动作具有高风险，且可能无法回退。`
5. 关键快照字段：`completion_status=confirmation_required；result_summary=需要人工确认后才能继续；未进入 action_requested。`
6. 是否通过：`通过`

### 样本 F：记忆召回可见性

1. 请求体：`继续总结我们当前项目主口径`
2. run_id：`run-1775204134843-221`
3. 核心事件链：`run_started -> analysis_ready -> plan_ready -> memory_recalled -> action_requested -> action_completed -> verification_completed -> memory_written -> memory_write_skipped -> memory_write_skipped -> run_finished`
4. 最终答复：`根据当前工作区的文档信息，我们的项目主口径是 V2产品需求文档...`
5. 关键快照字段：`memory_digest 含 来源=docs/README.md | 类型=seed | 理由=基线记忆优先；EventTimeline / LogsPanel / ContextSidebar 已可直接消费。`
6. 是否通过：`通过`

### 样本 G：知识召回优先级

1. 请求体：`knowledge: 项目主口径 README 06-development`
2. run_id：`run-1775204156909-224`
3. 核心事件链：`run_started -> analysis_ready -> plan_ready -> memory_recalled -> action_requested -> action_completed -> verification_completed -> memory_written -> memory_write_skipped -> memory_write_skipped -> run_finished`
4. 最终答复：`本地知识检索结果中，README 命中排在前，06-development 次之，并显式展示来源类型与命中理由。`
5. 关键快照字段：`第 1/2 条 reason=README 主入口优先；第 3 条 reason=06-development 开发文档优先；result_summary=已从本地知识源返回 3 条摘要结果。`
6. 是否通过：`通过`

### 专项样本 H：受控恢复正式留证

1. 请求体：`继续说刚才主链路还差什么`
2. run_id：`run-1775204233235-227`
3. 核心事件链：`run_started -> analysis_ready -> plan_ready -> memory_recalled -> action_requested -> action_completed -> verification_completed -> memory_written -> memory_written -> memory_write_skipped -> run_finished`
4. 最终答复：`主回答未成功，已切换到会话摘要恢复路径。当前没有可复用的会话摘要，建议补充更具体的问题或先提供上下文。`
5. 关键快照字段：`model.provider_id=invalid-provider；verification_code=verified_with_recovery；verification_summary=验证通过（受控恢复）：模型主回答失败，已执行单次恢复：provider_not_configured: provider 未配置；completion_reason=当前任务经单次受控恢复后通过验证。`
6. 是否通过：`通过`

---

## 11. 构建与运行验证

本轮必须重复确认以下构建：

1. `cargo build`
2. `go build ./...`
3. `npm run build`

当前结果：

1. `cargo build`：`通过`
2. `go build ./...`：`通过`
3. `npm run build`：`通过`

补充说明：

1. `run-mainline-acceptance.ps1` 已完整复跑成功。
2. `runtime-host` 健康检查通过：`2026-04-03 16:17 http://127.0.0.1:8898/health -> {"ok":true,"name":"runtime-host","version":"0.1.0"}`
3. `gateway` 健康检查通过：`2026-04-03 16:16 http://127.0.0.1:8897/health -> {"app":"本地智能体","gateway":8897,"status":"ok"}`
4. `runtime-host` 占用冲突专项检查通过：`tmp/runtime-host-lock-check.json 已生成；lock_detected=true；lock_mode=remove_failed。`
5. 手工交互回归未做完整浏览器逐页点击；本轮前端验收以代码核对、构建通过、真实运行事件留证为主。

---

## 12. 未完成项与边界说明

必须说明：

1. 本轮明确做了什么
2. 本轮明确没做什么
3. 哪些问题仍属于非阻断项

实际填写：

1. 已完成：`A 误路由修复；project_answer / context_answer 单次受控恢复；verify.rs / completion.rs 正式收口；README / 06-development 优先、07-test 降权；A-G 固定样本复跑；受控恢复专项样本 H 留证；runtime-host 占用冲突专项脚本留证；前端默认入口/空态/详情消费收口；三项构建通过。`
2. 未完成：`未做完整浏览器式逐页手工点击回归；未把执行阶段恢复触发权完全迁移为 verify.rs 统一调度。`
3. 非阻断项：`当前 verify.rs 已完成正式状态编码与收口留证，execution.rs 仍保留恢复触发判定；该实现满足本阶段主链路验收要求，不阻断下一阶段继续推进。`

---

## 13. 最终结论

### 13.1 当前判断

1. `主链路成功率收口`：`通过`
2. `默认路径收口`：`通过`
3. `记忆与知识质量收口`：`通过`
4. `最小验收基线`：`通过`
5. `本阶段总体验收结论`：`通过`

### 13.2 原因

实际填写：

1. `自然语言主链路误路由已明显收口，样本 A 已稳定命中 project_answer，样本 B 已稳定命中 session_context。`
2. `project_answer / context_answer 的单次恢复链已可用；专项样本 H 已真实产出 verified_with_recovery 与 completion_reason=当前任务经单次受控恢复后通过验证。`
3. `runtime-host 占用冲突已形成独立专项留证，tmp/runtime-host-lock-check.json 已真实记录 remove_failed 锁冲突，不再只停留在口头说明。`
4. `README / 06-development 优先、07-test 降权在样本 G 中已稳定生效；A-G 固定样本与三项构建在 2026-04-03 已完成正式复跑。`

---

## 14. 是否允许进入下一阶段

实际填写：

1. 是否允许进入下一阶段：`允许`
2. 原因：`本轮总体验收结论为“通过”；主链路成功率、默认路径、记忆与知识质量、最小验收基线均已达到当前阶段要求。`
