# 本地智能体 - 工作区与授权策略产品化验收文档 V1

更新时间：2026-04-02

状态：`当前有效`

执行标记：

1. `当前工作区与授权策略阶段正式验收模板`
2. `实现完成后必须按本文逐项填写`
3. `是否允许进入前端产品化收口阶段，以本文结论为准`

关联文档：

1. [开发文档收口导航_V1](D:/newwork/本地智能体/docs/06-development/开发文档收口导航_V1.md)
2. [V2产品需求文档](D:/newwork/本地智能体/docs/10-v2/V2产品需求文档.md)
3. [需求冻结稿_V1](D:/newwork/本地智能体/docs/00-charter/需求冻结稿_V1.md)

---

## 1. 文档目的

本文只回答 4 个问题：

1. 工作区与授权策略是否已经达到产品文档要求
2. 当前工作区、目录授权、风险设置是否已经真实生效
3. 当前实现是否允许进入前端产品化收口阶段
4. 还剩哪些阻断项和非阻断项

本文件不是新的开发任务书。
本文件是当前阶段的正式验收口径和填写模板。

---

## 2. 填写说明

实现完成后必须按以下规则填写：

1. 每一节都要写
2. 必须写清楚“做了什么、没做什么、怎么验证的”
3. 必须给出文件落点
4. 必须写明构建结果和手工测试结果
5. 必须给出明确结论：`通过 / 有条件通过 / 不通过`

---

## 3. 本轮目标

实际填写：

1. `将工作区切换、目录授权记忆、撤销与设置开关收口为真实生效链路。`
2. `完成设置接口、前端设置页与 Rust 风险确认链路的联调验收。`

---

## 4. 实现范围

实际填写：

1. `Go 设置状态从 approved_workspace_ids 扩展为 approved_directories 目录授权对象。`
2. `设置接口返回 approved_directories，并支持 revoke_directory_root 撤销。`
3. `前端设置页已记住授权列表改为目录授权列表，撤销动作改为按 root_path 生效。`
4. `工作区切换后 chat/run 继续透传新的 workspace_ref，由运行时以新工作区根目录执行。`
5. `按目录提醒开关与风险等级开关完成真实联调验证。`

---

## 5. 修改文件清单

实际填写：

1. `gateway/internal/state/store.go`
2. `gateway/internal/api/router.go`
3. `frontend/src/shared/contracts.ts`
4. `frontend/src/settings/api.ts`
5. `frontend/src/settings/useSettings.ts`
6. `frontend/src/settings/SettingsPanel.tsx`
7. `frontend/src/App.tsx`

---

## 6. 工作区能力验收

### 6.1 当前工作区显示

必须说明：

1. 首页是否轻量显示当前工作区
2. 设置页是否完整显示当前工作区路径与状态

实际填写：

1. 首页显示：`通过。TopBar 与 WorkbenchOverview 都轻量显示当前工作区名称，首页未扩展成重型工作区管理台。`
2. 设置页显示：`通过。Settings -> Workspace Roots 显示当前工作区、当前路径、可切换工作区列表与已记住授权列表。`

### 6.2 工作区切换

必须说明：

1. 是否支持切换当前工作区
2. 切换后前后端状态是否一致
3. 切换后运行时是否使用新工作区

实际填写：

1. 切换能力：`通过。POST /api/v1/settings { workspace_id } 后，settings.workspace 切换为目标工作区。`
2. 前后端状态一致性：`通过。设置接口返回、data/settings/ui-state.json 落盘、前端 SettingsResponse 字段均同步为新工作区。`
3. 运行时生效情况：`通过。acceptance-workspace-1 / 8 等联调日志中，repo_context_summary 与 workspace_root 均切到 D:/newwork/本地智能体/docs。`

### 6.3 可选工作区与已授权工作区

必须说明：

1. 是否能看到可选工作区列表
2. 是否能看到已记住授权的工作区或目录

实际填写：

1. 可选工作区：`通过。settings.available_workspaces 返回 main/docs 两个对象，设置页可直接切换。`
2. 已授权列表：`通过。settings.approved_directories 返回目录授权对象，前端按 name + root_path 展示。`

---

## 7. 目录授权能力验收

### 7.1 新目录首次接触提醒

必须说明：

1. 首次接触新目录时是否提示
2. 是否可继续或取消
3. 是否可记住此授权

实际填写：

1. 首次提醒：`通过。acceptance-workspace-1 与 acceptance-workspace-8 在 docs 未授权时均产生 confirmation_required(kind=workspace_access)。`
2. 继续 / 取消：`通过。/api/v1/chat/confirm 已支持 approve / reject / cancel，approve 后继续执行，reject/cancel 直接发布结束事件。`
3. 记住授权：`通过。对 confirm-workspace-run-1775180944181-2 提交 remember=true 后，approved_directories 增加 docs。`

### 7.2 已记住授权管理

必须说明：

1. 是否支持查看已记住授权
2. 是否支持撤销
3. 撤销后是否立即生效

实际填写：

1. 查看能力：`通过。设置接口与设置页均可查看已记住目录授权列表。`
2. 撤销能力：`通过。POST /api/v1/settings { revoke_directory_root } 可按目录根路径撤销。`
3. 生效情况：`通过。撤销 docs 后，settings.approved_directories 只剩 main；随后 acceptance-workspace-8 再次触发 confirmation_required。`

---

## 8. 风险与授权设置验收

### 8.1 目录提醒开关

必须说明：

1. 是否存在目录提醒开关
2. 开关是否真实生效

实际填写：

1. 开关存在情况：`通过。设置页 Risk & Permissions 区块存在“新目录首次接触提醒”开关。`
2. 生效情况：`通过。directory_prompt_enabled=false 后，运行 docs 工作区不再触发 workspace_access 确认，日志中 requires_confirmation=false。`

### 8.2 风险等级展示开关

必须说明：

1. 是否存在风险等级展示开关
2. 开关是否真实生效

实际填写：

1. 开关存在情况：`通过。设置页 Risk & Permissions 区块存在“显示风险等级”开关。`
2. 生效情况：`通过。设置值已真实回写到 /api/v1/settings 与 data/settings/ui-state.json；页面级联调中，show_risk_level=true 时确认卡头部显示 medium 风险标签，show_risk_level=false 后再次触发确认卡时风险标签消失，已完成浏览器级留证。`

### 8.3 设置页区块收口

必须说明：

1. 工作区设置区块是否完整
2. 风险与授权策略区块是否完整

实际填写：

1. 工作区设置区块：`通过。包含当前工作区、当前路径、可切换工作区列表、已记住授权列表、移除授权动作。`
2. 风险与授权策略区块：`通过。包含目录提醒开关、风险等级展示开关和已授权范围撤销机制。`

---

## 9. API 与状态验收

### 9.1 设置接口返回

必须说明：

1. 是否返回当前工作区
2. 是否返回可选工作区
3. 是否返回已授权工作区或目录
4. 是否返回目录提醒与风险等级展示开关

实际填写：

1. 当前工作区：`通过。返回 workspace。`
2. 可选工作区：`通过。返回 available_workspaces。`
3. 已授权对象：`通过。返回 approved_directories，字段包含 approval_id / workspace_id / name / root_path。`
4. 开关字段：`通过。返回 directory_prompt_enabled / show_risk_level。`

### 9.2 状态持久化

必须说明：

1. 工作区切换是否会持久化
2. 授权策略是否会持久化

实际填写：

1. 工作区持久化：`通过。workspace_id 写入 data/settings/ui-state.json。`
2. 授权策略持久化：`通过。approved_directories、approved_workspace_ids、两个开关均持久化到 data/settings/ui-state.json。`

---

## 10. UI 位置验收

必须说明：

1. 首页是否只轻量显示工作区
2. 是否没有做成首页重型工作区管理台
3. 设置页是否承担完整管理职责

实际填写：

1. 首页轻量显示：`通过。首页只显示工作区摘要与入口，没有扩成管理台。`
2. 首页是否失控扩张：`通过。工作区完整管理仍在设置页，不在首页承载。`
3. 设置页管理职责：`通过。切换、授权查看/撤销、风险开关都集中在设置页。`

---

## 11. 手工测试记录

至少覆盖 6 类场景：

### 11.1 场景一：工作区切换

1. 输入：`待填写`
2. 预期：`待填写`
3. 实际结果：`待填写`

实际填写：

1. 输入：`POST /api/v1/settings {"workspace_id":"docs"}`
2. 预期：`settings.workspace 切为 docs，后续运行使用 docs 根目录。`
3. 实际结果：`通过。接口返回 workspace_id=docs，后续 acceptance-workspace-1/8 日志的 workspace_root 为 D:/newwork/本地智能体/docs。`

### 11.2 场景二：切换后运行时生效

1. 输入：`待填写`
2. 预期：`待填写`
3. 实际结果：`待填写`

实际填写：

1. 输入：`POST /api/v1/chat/run，workspace 指向 docs`
2. 预期：`Rust 运行时按 docs 工作区装配 repo context。`
3. 实际结果：`通过。日志 repo_context_summary / context_snapshot.workspace_root 均为 D:/newwork/本地智能体/docs。`

### 11.3 场景三：首次接触新目录提醒

1. 输入：`待填写`
2. 预期：`待填写`
3. 实际结果：`待填写`

实际填写：

1. 输入：`撤销 docs 授权后运行 acceptance-workspace-8`
2. 预期：`首次接触 docs 时触发 confirmation_required(kind=workspace_access)。`
3. 实际结果：`通过。run-1775181735141-8 第 4 条事件为 confirmation_required，risk_level=medium，kind=workspace_access。`

### 11.4 场景四：记住授权

1. 输入：`待填写`
2. 预期：`待填写`
3. 实际结果：`待填写`

实际填写：

1. 输入：`POST /api/v1/chat/confirm {"confirmation_id":"confirm-workspace-run-1775180944181-2","decision":"approve","remember":true}`
2. 预期：`docs 被加入已记住授权列表，后续不再提示。`
3. 实际结果：`通过。settings.approved_directories 增加 docs；acceptance-workspace-2/3 未再触发 confirmation_required，直接 run_finished。`

### 11.5 场景五：撤销授权

1. 输入：`待填写`
2. 预期：`待填写`
3. 实际结果：`待填写`

实际填写：

1. 输入：`POST /api/v1/settings {"revoke_directory_root":"D:/newwork/本地智能体/docs"}`
2. 预期：`docs 从已授权列表删除，并在下次进入时重新提示。`
3. 实际结果：`通过。settings.approved_directories 只剩 main，ui-state.json 同步移除 docs；acceptance-workspace-8 再次触发 confirmation_required。`

### 11.6 场景六：风险等级展示开关

1. 输入：`待填写`
2. 预期：`待填写`
3. 实际结果：`待填写`

实际填写：

1. 输入：`POST /api/v1/settings {"directory_prompt_enabled":false,"show_risk_level":false}`
2. 预期：`设置回写成功；目录提醒关闭后 docs 直通；风险标签前端按开关隐藏。`
3. 实际结果：`通过。接口与 ui-state.json 均回写 false；关闭提醒后运行 docs 未再触发确认。页面级联调中再次触发确认卡后，卡头仅保留 Risk Gate 与“需要确认后继续”，medium 风险标签不再显示。`

---

## 12. 构建与运行验证

实际填写：

1. `cargo build`：`未执行。本轮未修改 Rust 代码。`
2. `go build ./...`：`通过。`
3. `npm run build`：`通过。`

---

## 13. 偏离任务书的地方

实际填写：

1. `新目录首次接触当前按“工作区根目录”对象收口，而不是任意子目录细粒度授权。该实现符合当前任务书“工作区与授权策略产品化”范围。`
2. `runtime_status 曾存在瞬时探活抖动，本轮已在 gateway 设置接口查询中补 3 次短重试，降低设置页短暂显示 unreachable 的概率。`

---

## 14. 未完成项

### 14.1 阻断项

1. `无`

### 14.2 非阻断项

1. `无`

---

## 15. 验收结论

### 15.1 当前正式结论

1. 结论：`通过`
2. 验收日期：`2026-04-03`
3. 验收人：`Codex`

### 15.2 结论理由

1. `工作区切换、目录首次提醒、记住授权、撤销后立即生效、目录提醒开关真实生效，核心主链路已满足任务书。`
2. `设置页、首页、接口返回和持久化状态已对齐，未出现“只切 UI 不切实际工作区”的假切换。`
3. `风险等级展示开关已完成浏览器级联调验证；show_risk_level 开关开启与关闭两种状态都已在真实确认卡上得到验证。`
4. `runtime_status 瞬时探活问题已补短重试收口，当前阶段不存在阻断项与待补 UI 留证项。`

---

## 16. 是否允许进入下一阶段

1. 结论：`允许`
2. 原因：`本阶段正式结论为“通过”，任务书要求的工作区与授权策略产品化能力已完成正式验收。`
