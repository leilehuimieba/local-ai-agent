# 本地智能体 - 第二阶段短期可用能力验收文档 V1

更新时间：2026-04-04

状态：`当前有效`

执行标记：

1. `第二阶段短期可用能力正式验收模板`
2. `实现完成后必须按本文逐项填写`
3. `是否允许判定第二阶段通过，以本文结论为准`

关联文档：

1. [第二阶段产品定位与开发重点清单_V1](D:/newwork/本地智能体/docs/06-development/第二阶段产品定位与开发重点清单_V1.md)
2. [第二阶段短期可用能力开发任务书_V1](D:/newwork/本地智能体/docs/06-development/第二阶段短期可用能力开发任务书_V1.md)
3. [产品级冻结与下一阶段规划入口文档_V1](D:/newwork/本地智能体/docs/06-development/产品级冻结与下一阶段规划入口文档_V1.md)
4. [智能体框架主干开发任务书_V1](D:/newwork/本地智能体/docs/06-development/智能体框架主干开发任务书_V1.md)

---

## 1. 文档目的

本文只回答 4 个问题：

1. 第二阶段短期可用目标是否已经成立。
2. 当前系统是否已达到“接入 API 后足够可用、可执行、可沉淀、可长期使用”的门槛。
3. 当前实现是否允许继续进入下一阶段能力建设。
4. 还剩哪些非阻断项、后置项和明确风险。

本文件不是新的开发任务书。
本文件是第二阶段短期能力的正式验收口径和填写模板。

---

## 2. 填写说明

实现完成后，必须按以下规则填写：

1. 每一节都要写，不允许只写一句“已完成”。
2. 必须写清楚“做了什么、没做什么、怎么验证的”。
3. 必须给出具体文件落点。
4. 必须写明构建结果和手工验证结果。
5. 每一大项都必须给出明确结论：`通过 / 有条件通过 / 不通过`。
6. 整体结论只允许：`通过 / 有条件通过 / 不通过`。

---

## 3. 本轮目标

填写要求：

1. 用 1 到 3 句话写清本轮验收目标。
2. 明确本轮只验收什么，不验收什么。

实际填写：

1. 本轮目标是把本地智能体收口到“接入在线模型 API 后，可以稳定对话、真实读写文件、执行明确操作、具备本地缓存、可持续沉淀”的状态。
2. 本轮只验收短期可用能力，不验收双智能体、语音、浏览器全局观察、桌面自动化和重型平台化能力。

---

## 4. 实现范围

填写要求：

1. 只列出本轮实际完成的能力点。
2. 不要写计划项，只写已实现项。

实际填写：

1. 在线模型接入后的自然语言主链路已经可运行，项目说明类问题会进入 `project_answer`，会话续答类问题会进入 `context_answer`，能力说明类问题会进入 `explain`。
2. 工作区内文件读取、文件写入、命令执行已经可以真实完成，并能在事件流和稳定日志中看到执行结果、验证结果与最终收口。
3. `project_answer` 路径已接入本地回答缓存，首轮样本可见 `miss`，同问重跑样本可见 `hit`，缓存原因和思考摘要会进入事件与日志详情。
4. 记忆召回、工作记忆落盘、失败教训写入、低价值写入拦截仍在正式主链路内，且 `README / 06-development` 继续保持高优先级。
5. 前端事件调查层与日志页已经直接消费 `context_snapshot` 中的思考摘要、缓存状态和缓存原因，不再只依赖黑盒 metadata。

---

## 5. 修改文件清单

填写要求：

1. 只列出本轮实际修改或新增的文件。
2. 路径必须具体。

实际填写：

1. `crates/runtime-core/src/query_engine.rs`
2. `crates/runtime-core/src/execution.rs`
3. `crates/runtime-core/src/prompt.rs`
4. `crates/runtime-core/src/storage.rs`
5. `frontend/src/events/EventTimeline.tsx`
6. `frontend/src/logs/LogsPanel.tsx`
7. `docs/07-test/第二阶段短期可用能力验收文档_V1.md`
8. `tmp/phase2-acceptance/phase2-samples.json`
9. `tmp/phase2-acceptance/sample-dialog-events.json`
10. `tmp/phase2-acceptance/sample-cache-events.json`
11. `tmp/phase2-acceptance/sample-read-events.json`
12. `tmp/phase2-acceptance/sample-write-events.json`
13. `tmp/phase2-acceptance/sample-command-events.json`
14. `tmp/phase2-acceptance/sample-dialog-rerun-summary.json`
15. `tmp/phase2-acceptance/sample-dialog-rerun-events.json`
16. `tmp/phase2-acceptance/sample-cache-rerun-events.json`
17. `tmp/phase2-acceptance/route-samples.json`
18. `tmp/phase2-acceptance/project-answer-fix-summary.json`
19. `tmp/phase2-acceptance/project-answer-fix-first.json`
20. `tmp/phase2-acceptance/project-answer-fix-second.json`
21. `tmp/phase2-acceptance/project-answer-status-summary.json`
22. `tmp/phase2-acceptance/project-answer-status-first.json`
23. `tmp/phase2-acceptance/project-answer-status-second.json`

---

## 6. 总体验收结论页

### 6.1 结论类型

总体验收只允许以下 3 种结论：

1. `通过`
2. `有条件通过`
3. `不通过`

### 6.2 当前正式结论

1. 结论：`通过`
2. 验收日期：`2026-04-04`
3. 验收人：`Codex`

### 6.3 结论理由

填写要求：

1. 用 3 到 6 条写清结论依据。
2. 结论依据必须是已实现和已验证的事实。

实际填写：

1. `cargo build`、`go build ./...`、`npm run build` 均已通过，说明三端构建链路成立。
2. 真实样本已覆盖自然语言对话、缓存命中、文件读取、文件写入、命令执行、能力说明和高风险确认，证据已留存在 `tmp/phase2-acceptance/`。
3. 工作区内文件读写与命令执行均返回真实结果，写入文件 `tmp/phase2-acceptance/generated-note.txt` 已落盘，命令执行样本返回成功收口。
4. `project_answer` 路径已经接入本地缓存，最新 `phase2-project-answer-status` 样本首轮可见 `cache_status=miss`，同问重跑可见 `cache_status=hit`，缓存原因与思考摘要可见。
5. 事件流和稳定日志已具备 `memory_digest`、`knowledge_digest`、`reasoning_summary`、`verification_summary` 等正式字段，前端已直接消费这些字段。
6. 最新 `project_answer` 状态类样本已经优先引用 `第二阶段需求文档 / 产品定位与开发重点清单 / 短期可用能力开发任务书 / 短期可用能力验收文档` 四份核心文档，不再只依赖 `docs/README.md` 的导航摘要。
7. 当前系统已达到“可用、可执行、可沉淀”的短期门槛；最新 `project_answer` 样本已能按“已完成能力 / 当前阶段 / 待收口项”直接稳定收口，并明确说明这些能力已有真实样本留证，因此总体验收调整为通过。

---

## 7. 工作包 A 验收：在线模型对话主链路

### 7.1 对话主链路是否成立

必须说明：

1. 用户是否可以稳定发起对话。
2. 在线模型是否已进入正式主链路。
3. 对话是否仍大量落入错误路径。

实际填写：

1. 用户已经可以稳定发起对话，`phase2-accept-dialog-rerun`、`phase2-accept-context`、`phase2-accept-explain`、`phase2-project-answer-status` 四个会话都完成了真实收口。
2. 在线模型已进入正式主链路，`project_answer`、`context_answer`、`runtime_output` 都通过 runtime 调用模型，并在失败时进入受控恢复。
3. 当前对话不会大量落入错误路径，最新 `phase2-project-answer-status` 样本中 `project_answer` 首答已经可直接按“已完成能力 / 当前阶段 / 待收口项”完成收口，第二轮同问直接命中缓存；当前问题主要转为“细项还可更展开”，而不是主链路失稳。

### 7.2 `project_answer / context_answer / explain` 路由情况

必须说明：

1. 项目说明类问题是否优先进入 `project_answer`。
2. 会话续答类问题是否优先进入 `context_answer`。
3. 能力说明类问题是否仍可进入 `explain`。

实际填写：

1. 项目说明类问题“这个项目现在做到什么程度了”在 `phase2-accept-dialog-rerun` 与 `phase2-project-answer-status` 会话中都进入 `project_answer`，工具名为 `project_answer`。
2. 会话续答类问题“继续说刚才主链路还差什么”在 `phase2-accept-context` 会话中进入 `context_answer`，工具名为 `session_context`。
3. 能力说明类问题“你现在支持什么能力”在 `phase2-accept-explain` 会话中进入 `explain`，工具名为 `runtime_output`。

### 7.3 主回答失败后的受控恢复

必须说明：

1. 是否存在单次受控恢复。
2. 恢复依据来自哪里。
3. 恢复是否可在事件与日志中看见。

实际填写：

1. 当前存在单次受控恢复，`context_answer` 在 `phase2-accept-context` 样本中触发会话摘要恢复；`project_answer` 在旧样本中出现过恢复路径，但最新 `phase2-project-answer-fix` 与 `phase2-project-answer-status` 样本已可直接通过主回答收口。
2. 恢复依据来自本地项目文档片段或当前会话压缩摘要；当模型输出不可用时，会降级到 `fallback_project_summary` 或 `fallback_context_summary`。
3. 恢复留痕可在事件与日志中直接看到，证据包括 `verification_code=verified_with_recovery`、`verification_summary=验证通过（受控恢复）...`、`reasoning_summary=模型回答不可用，已降级为...`。

### 7.4 本工作包结论

1. 结论：`通过`
2. 结论理由：`主链路已经成立，三类自然语言路由均已跑通；恢复链路仍保留，但当前更多用于兜底而不是主路径必经步骤。`

---

## 8. 工作包 B 验收：文件读写与执行能力

### 8.1 文件读取

必须说明：

1. 是否能在工作区内真实读取文件。
2. 读取结果是否回流给用户。
3. 读取失败时是否有明确信息。

实际填写：

1. 可以在工作区内真实读取文件，样本 `read: docs/README.md` 已成功读取 [README.md](D:/newwork/本地智能体/docs/README.md)。
2. 读取结果已回流到最终答复和日志详情，`result_summary` 为“文件读取成功，摘要：...”，并带有验证摘要。
3. 读取失败场景未在本轮样本中单独构造，但执行分支已具备明确错误文本返回，路径越界和文件系统错误会直接写入 `final_answer` 与 `result_summary`。

### 8.2 文件写入

必须说明：

1. 是否能在工作区内真实写入文件。
2. 写入结果是否可验证。
3. 路径边界是否仍有效。

实际填写：

1. 可以在工作区内真实写入文件，样本 `write: tmp/phase2-acceptance/generated-note.txt` 已写入成功。
2. 写入结果可验证，目标文件 [generated-note.txt](D:/newwork/本地智能体/tmp/phase2-acceptance/generated-note.txt) 的实际内容为“第二阶段验收写入样本。用于验证工作区内真实写文件能力。”。
3. 路径边界仍有效，写入能力继续通过工作区路径解析函数收口，未突破当前工作区根目录。

### 8.3 命令执行

必须说明：

1. 是否能执行明确、受控的本地命令。
2. 输出或错误是否能回流。
3. 高风险命令是否仍受确认链路约束。

实际填写：

1. 可以执行明确、受控的本地命令，样本 `cmd: cargo build` 已成功完成。
2. 命令输出或错误会回流到 `final_answer` 和 `result_summary`，本轮样本返回“命令执行成功，输出摘要：无可显示内容。”。
3. 高风险命令与高风险删除仍受确认链路约束，样本 `delete: tmp/phase2-acceptance/risk-dir` 返回 `confirmation_required`，风险级别为 `irreversible`。

### 8.4 本工作包结论

1. 结论：`通过`
2. 结论理由：`文件读取、文件写入、命令执行都已真实落地并留证，高风险动作仍会暂停并要求确认。`

---

## 9. 工作包 C 验收：思维链路与结果解释

### 9.1 事件与日志是否具备关键上下文

必须说明：

1. 是否能看到上下文摘要。
2. 是否能看到记忆摘要。
3. 是否能看到知识摘要。
4. 是否能看到验证摘要。

实际填写：

1. 事件与日志中可以直接看到上下文摘要，`context_snapshot.session_summary` 与 `workspace_root` 已稳定返回。
2. 记忆摘要可见，`memory_digest` 在对话、读写、命令样本中都已进入正式字段。
3. 知识摘要可见，`knowledge_digest` 在项目说明样本和文件读写样本中已稳定返回。
4. 验证摘要可见，`verification_snapshot.summary` 在对话、读写、命令样本中都可以直接查看。

### 9.2 思考摘要与状态解释

必须说明：

1. 是否能看到思考摘要。
2. 是否能区分正常回答、恢复路径、失败路径。
3. 用户是否能理解“为什么这样回答或这样执行”。

实际填写：

1. 可以看到思考摘要，`reasoning_summary` 已在事件与日志详情区展示，并进入缓存搜索条件。
2. 可以区分正常回答、恢复路径、失败路径和缓存命中：本轮样本中出现了 `cache_status=miss`、`cache_status=hit`、`verification_code=verified_with_recovery`、`confirmation_required` 等不同状态。
3. 用户已经能够理解系统为何这样回答或执行，因为日志中同时给出 `reasoning_summary`、`cache_reason`、`verification_summary` 和 `repo_context_summary`。

### 9.3 前端详情区消费情况

必须说明：

1. 前端是否仍只依赖 metadata。
2. 正式字段是否已经被详情区直接消费。
3. 展示是否足以支持人工复盘。

实际填写：

1. 前端不再只依赖 metadata，`EventTimeline` 与 `LogsPanel` 已直接消费 `context_snapshot.reasoning_summary`、`context_snapshot.cache_status`、`context_snapshot.cache_reason`。
2. 正式字段已经进入详情区和时间线标签展示，同时仍兼容 metadata 作为补充来源。
3. 当前展示已经足以支持人工复盘，至少可以直接观察工作区上下文、记忆依据、知识依据、思考摘要、缓存状态和验证结果。

### 9.4 本工作包结论

1. 结论：`通过`
2. 结论理由：`解释性字段已经进入正式事件结构，并且前端已直接消费，人工复盘所需核心信息已具备。`

---

## 10. 工作包 D 验收：本地缓存与成本优化

### 10.1 本地缓存是否进入主链路

必须说明：

1. 哪些路径已接入缓存。
2. 缓存写入与读取是否真实发生。
3. 缓存是否仍保持轻量实现。

实际填写：

1. 当前已接入缓存的主路径是 `project_answer`，最新对话样本 `phase2-project-answer-status` 首轮为 `miss`，同问重跑样本为 `hit`。
2. 缓存写入和读取都真实发生：首轮样本结束后写入 `data/cache/main.jsonl`，第二轮同问直接命中并复用稳定回答。
3. 当前缓存仍保持轻量实现，载体是本地 JSONL 回答缓存，没有引入额外缓存中台或分布式系统。

### 10.2 缓存命中可见性

必须说明：

1. 是否能看到缓存命中。
2. 是否能看到缓存未命中的原因。
3. 是否能在事件或日志中留证。

实际填写：

1. 能看到缓存命中，`project-answer-status-second.json` 与 `sample-cache-rerun-events.json` 中都可见 `cache_status=hit`。
2. 能看到缓存未命中的原因，`project-answer-status-first.json` 与 `sample-dialog-rerun-events.json` 中都可见 `cache_status=miss` 与“本轮将生成新回答并回写缓存”。
3. 缓存命中和未命中都已经在事件和稳定日志中留证，且前端日志页和事件调查层已显示这些字段。

### 10.3 成本优化是否形成真实效果

必须说明：

1. 是否减少了重复回答。
2. 是否减少了重复检索或重复思考。
3. 当前优化是否已对真实使用有价值。

实际填写：

1. 已减少重复回答，`phase2-project-answer-status` 第二轮同问不再重新生成项目说明，而是直接走本地缓存命中路径。
2. 已减少重复检索与重复思考，最新 `reasoning_summary` 明确指出当前输入与项目文档摘要稳定命中缓存，直接复用最近回答。
3. 这类优化对真实使用已经有价值，尤其适用于重复追问项目说明、项目状态和其他稳定问答场景。

### 10.4 本工作包结论

1. 结论：`通过`
2. 结论理由：`本地缓存已经进入项目说明主路径，命中和未命中均可见且有真实复用效果。`

---

## 11. 工作包 E 验收：记忆与知识沉淀继续增强

### 11.1 用户偏好与失败教训沉淀

必须说明：

1. 用户偏好是否继续进入正式主链路。
2. 失败教训是否继续进入正式主链路。
3. 是否存在低质量写入拦截。

实际填写：

1. 用户偏好与项目规则仍在正式主链路中，`memory_digest` 多次召回了“优先使用 docs/README.md 与 docs/06-development”的项目规则。
2. 失败教训继续进入正式主链路，`sample-dialog-rerun-events.json` 中出现了“失败教训：项目问答 遇到异常时应执行单次受控恢复并保留恢复留痕。”的长期记忆写入。
3. 存在低质量写入拦截，本轮多次出现 `memory_write_skipped`，原因是命中低价值运行时记忆治理规则或知识治理规则。

### 11.2 知识召回质量

必须说明：

1. 是否继续保持 `README / 06-development 优先、07-test 降权`。
2. 召回是否具备来源和理由。
3. 历史脏数据治理是否继续推进。

实际填写：

1. 当前仍保持 `README / 06-development 优先、07-test 降权`，这既体现在 `memory_digest` 的项目规则，也体现在 `knowledge_digest` 的命中结果中。
2. 召回具备来源和理由，`memory_recalled` 事件会记录 `source_type`、`record_type`、`reason`，知识摘要会保留来源文件路径。
3. 历史脏数据治理继续推进，本轮样本中低价值运行时知识写入被拦截，没有继续污染长期知识层。

### 11.3 本工作包结论

1. 结论：`通过`
2. 结论理由：`记忆召回、失败教训写入和低质量拦截都仍在主链路内，知识优先级没有回退。`

---

## 12. 工作包 F 验收：专业能力与本地兜底预留

### 12.1 skill / 专业能力接口预留

必须说明：

1. 是否已明确 skill 接入口位置。
2. 当前是否仍保持不破坏主链路。
3. 后续 CTF / 网络安全方向是否已有清晰落点。

实际填写：

1. 已明确 skill 接入口位置，当前落点仍是 `crates/runtime-core/src/tools.rs`、`crates/runtime-core/src/tool_registry.rs`。
2. 当前保持不破坏主链路，skill 仍以“工具定义与注册预留”为主，没有引入新的重执行域。
3. 后续 CTF / 网络安全方向已有清晰落点，可沿 `tool_registry + tools + planner` 继续接入。

### 12.2 本地小模型兜底预留

必须说明：

1. 是否已明确本地小模型的定位。
2. 是否已明确本地小模型适合承担的能力范围。
3. 是否仍保持“只预留、不重做”的边界。

实际填写：

1. 已明确本地小模型的定位是低成本兜底执行模型，而不是复杂规划模型，这一点仍与第二阶段需求文档保持一致。
2. 当前已明确其适合承担简单说明、规则执行、日志理解、配置切换、已有工具调用等轻量能力范围。
3. 当前仍保持“只预留、不重做”的边界，本轮没有新增本地小模型正式主链路。

### 12.3 本工作包结论

1. 结论：`通过`
2. 结论理由：`专业能力与本地兜底都已保留明确落点，且没有破坏当前短期主链路。`

---

## 13. 构建与运行验证

必须填写以下验证结果：

1. `cargo build`
2. `go build ./...`
3. `npm run build`
4. 至少一轮真实对话样本
5. 至少一轮缓存命中样本
6. 至少一轮文件读写或命令执行样本

实际填写：

1. `cargo build`：`通过`。2026-04-04 已在仓库根目录执行成功。
2. `go build ./...`：`通过`。2026-04-04 已在 `gateway/` 目录执行成功。
3. `npm run build`：`通过`。2026-04-04 已在 `frontend/` 目录执行成功。
4. 真实对话样本：`通过`。证据为 `tmp/phase2-acceptance/sample-dialog-rerun-summary.json`、`tmp/phase2-acceptance/sample-dialog-rerun-events.json`、`tmp/phase2-acceptance/project-answer-fix-summary.json`、`tmp/phase2-acceptance/project-answer-status-summary.json`。
5. 缓存命中样本：`通过`。证据为 `tmp/phase2-acceptance/sample-cache-rerun-events.json`、`tmp/phase2-acceptance/project-answer-status-second.json`，其中可见 `cache_status=hit`。
6. 文件读写 / 命令执行样本：`通过`。证据为 `tmp/phase2-acceptance/sample-read-events.json`、`tmp/phase2-acceptance/sample-write-events.json`、`tmp/phase2-acceptance/sample-command-events.json`，实际写入文件为 [generated-note.txt](D:/newwork/本地智能体/tmp/phase2-acceptance/generated-note.txt)。

---

## 14. 当前未完成项

### 14.1 阻断项

填写要求：

1. 如果没有，明确写 `无`。
2. 如果有，必须写清是否阻断第二阶段通过。

实际填写：

1. `无`

### 14.2 非阻断项

填写要求：

1. 只写当前真实还存在但不阻断结论的问题。
2. 每条要简明说明影响范围。

实际填写：

1. `project_answer` 当前已经能稳定回答“已完成能力 / 当前阶段 / 待收口项”，后续仍可继续把每项能力细化到更具体的样本编号和完成标准。
2. `context_answer` 在缺少有效会话摘要时会退化为恢复提示，这符合当前边界，后续仍可继续增强会话压缩摘要厚度。

---

## 15. 最终验收结论

填写要求：

1. 必须明确写 `通过 / 有条件通过 / 不通过`。
2. 如果是 `有条件通过`，必须写清哪些问题不阻断下一阶段。
3. 如果是 `不通过`，必须写清阻断点是什么。

实际填写：

1. 结论：`通过`
2. 结论理由：`第二阶段短期目标要求的对话、读写、执行、解释、缓存、记忆沉淀已经全部可跑、可构建、可留证；最新 `project_answer` 状态类样本已能直接引用阶段二核心文档完成首答，并稳定形成“已完成能力 / 当前阶段 / 待收口项”加 `miss -> hit` 的缓存闭环，且已明确说明这些能力存在真实样本留证，因此第二阶段短期目标正式通过。`
