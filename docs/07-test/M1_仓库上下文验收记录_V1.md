# 本地智能体 - M1 仓库上下文验收记录 V1

更新时间：2026-04-01

状态：M1 验收已完成；本文为已落档的正式验收记录。

执行标记：
- `已完成`
- `只读归档`
- `禁止再次作为待填写模板重复使用`
- 若后续阶段需要验收，应新建新的阶段验收文档

适用对象：

1. 完成 M1 实现后的自验收
2. 由另一个对话或人工进行复验
3. 后续进入 M2 之前的阶段判定

关联文档：
- [M1_仓库上下文基线开发任务书_V1](D:/newwork/本地智能体/docs/06-development/M1_仓库上下文基线开发任务书_V1.md)
- [运行时优先能力工程实施规范_V1](D:/newwork/本地智能体/docs/06-development/运行时优先能力工程实施规范_V1.md)
- [优先能力实现顺序与落点_V1](D:/newwork/本地智能体/docs/06-development/优先能力实现顺序与落点_V1.md)
- [验收标准_V1](D:/newwork/本地智能体/docs/07-test/验收标准_V1.md)
- [V2产品需求文档](D:/newwork/本地智能体/docs/10-v2/V2产品需求文档.md)

---

## 1. 文档目的

本文件用于回答以下问题：

1. M1 到底做到什么程度才算完成
2. 需要验证哪些行为
3. 需要保留哪些验收证据
4. 哪些问题可以接受为降级
5. 哪些问题属于直接不通过

本文件不是实现说明，而是：

> M1 的正式验收口径与记录模板。

实现对话完成 M1 后，必须基于本文件补全真实验收结果，不允许只口头说明“已经差不多完成”。

---

## 2. M1 验收结论页

这一页应在验收完成后先填写，便于快速判断是否可进入 M2。

### 2.1 验收结论

请从以下三种状态中选择一种：

1. `通过`
2. `有条件通过`
3. `不通过`

### 2.2 当前结论

验收结论：

- `有条件通过`

验收日期：

- `2026-04-01`

验收人：

- `Codex（实现对话自验收）`

实现版本或提交标识：

- `未提交到 Git；基于 2026-04-01 当前工作区代码状态完成验收`

### 2.3 结论说明

请用 3 到 10 行填写：

1. 本轮已完成 M1 要求的独立 `repo_context` Rust 模块、主链路采集、planner 最小接入、事件 metadata 回流与前端右侧栏最小展示。
2. `cargo build`、`go build ./...`、`frontend npm run build` 全部通过，且在 ASCII 测试仓库中验证了标准 Git、非 Git、Git 局部失败三类核心场景。
3. 非 Git 场景和 Git 局部失败场景均未中断主链路，`repo_context_status` 正确表现为 `degraded`，并带有可解释的 warning。
4. 当前工作区本身不是 Git 仓库，右侧栏会按降级路径展示；这符合 M1 预期。
5. 存在 1 个非阻断问题：当前前端右侧栏基于事件 metadata 展示最小 repo context，但未单独展示说明文件摘要正文；字段与数量已正确，不阻断 M2 继续围绕 Context Envelope 设计。
6. 因此本轮不判定为“完全通过”，而判定为“有条件通过”，且问题不阻断进入 M2。

---

## 3. M1 验收对象

M1 验收只围绕仓库上下文基线能力，不验收 M2 及之后的内容。

### 3.1 必验对象

本轮已验收以下对象：

1. Rust `repo_context` 模块是否存在
2. runtime 主链路是否接入 repo context
3. planner 是否消费 repo context 摘要
4. 事件 metadata 是否回流 repo context 结果
5. 前端右侧上下文栏是否能显示最小 repo context 信息
6. 非 Git 目录和 Git 异常场景是否可降级

### 3.2 不属于本轮验收对象

以下内容即使已实现，也不作为 M1 通过依据：

1. Context Builder
2. Prompt 三层结构
3. Tool Registry
4. 结构化记忆 schema
5. Artifact 外置
6. Verification Chain
7. Model Adapter

---

## 4. M1 通过总标准

M1 要被判定为“通过”，至少必须同时满足以下 8 条：

1. Rust runtime 已形成独立 `repo_context` 模块
2. 每次 run 开始前都能形成 `RepoContextSnapshot`
3. Git 可用时，至少能拿到当前分支、dirty 状态、最近提交摘要
4. 非 Git 目录或 Git 失败时，主链路不会中断
5. 项目说明文件命中结果能形成轻量摘要
6. planner 已开始使用 repo context 摘要，而不是完全忽略
7. 事件 metadata 和前端右侧栏都能看到最小 repo context 信息
8. 实现过程中未引入 Python 或其他新的正式后端运行时依赖

本轮结论：

1. 上述 8 条核心标准均已满足
2. 但前端当前展示的是最小字段与摘要总览，而不是逐条说明文件摘要正文
3. 该问题不阻断主链路，也不阻断进入 M2，因此最终结论落为“有条件通过”

---

## 5. 有条件通过标准

只有在以下情况下，才允许判定为“有条件通过”：

1. M1 主链路已经打通
2. 所有核心结构已经落地
3. 至多存在 1 到 2 个非阻断性问题
4. 这些问题不影响进入 M2 的主干规划

本轮符合上述条件，具体说明如下：

1. 主链路已打通：`RunRequest -> repo_context 采集 -> planner 分析 -> 事件回流 -> 前端展示`
2. 核心结构已落地：`RepoContextSnapshot`、`GitSnapshot`、`GitCommitSummary`、`WorkspaceDocSummary`
3. 当前仅记录 1 个非阻断问题：前端右侧栏未展示每份说明文件的摘要正文，仅展示数量、路径、分支、dirty 状态和聚合摘要
4. 该问题不影响 M2 继续做 Context Envelope 与 Prompt 分层

---

## 6. 不通过标准

以下任一情况存在，即判定 M1 不通过：

1. `repo_context` 逻辑没有收口成独立 Rust 模块
2. 主链路 run 前没有正式采集 repo context
3. Git 不可用或非 Git 目录时直接报错中断
4. 说明文件全文或超长原文被直接塞进事件或 session
5. planner 没有任何 repo context 消费痕迹
6. 前端无最小展示能力
7. 合同未同步三端
8. 实现偏离 Go + Rust 主线

本轮未触发上述任一不通过条件。

---

## 7. 验收前置条件

开始验收前，以下条件必须满足。

### 7.1 构建条件

至少确认：

1. `cargo build` 通过
2. `go build ./...` 通过
3. `frontend` 的 `npm run build` 通过

验收记录：

- `2026-04-01` 已执行，三项全部通过。
- `cargo build` 首轮仅出现 1 个未使用 import 警告，随后已清理，复跑通过。
- `go build ./...` 在 `gateway/` 目录下通过。
- `npm run build` 在 `frontend/` 目录下通过，Vite 产物更新为 `dist/assets/index-DbpAAcuh.js`。

### 7.2 实现说明已提交

必须确认实现对话已提交：

1. 修改文件清单
2. 合同变更说明
3. 手工测试说明
4. 已知问题列表

验收记录：

- 修改文件清单已形成，见第 8 节与第 12 节。
- 合同变更说明已形成，见第 8.3 节与第 9 节。
- 手工测试说明已形成，见第 9 节。
- 已知问题列表已形成，见第 12 节。

### 7.3 不得混入后续里程碑主逻辑

必须确认：

1. 没有把 M2 的完整 Context Builder 作为 M1 主体交付
2. 没有把 Tool Registry 作为 M1 主体交付
3. 没有把其他语言依赖引入为正式运行依赖

验收记录：

- 已确认未混入 M2 的完整 Context Builder。
- 已确认未引入 Tool Registry 主逻辑。
- 已确认仍然维持 Go + Rust 主线，未新增 Python、Node 等正式后端依赖。

---

## 8. 结构验收

本节验收对象是否“存在、命名合理、职责正确”。

### 8.1 Rust 模块结构验收

必须确认：

1. 存在 `crates/runtime-core/src/repo_context.rs`
2. 模块职责主要集中在该文件
3. 没有把 repo context 主逻辑继续散落在 `lib.rs`

通过标准：

1. 新模块存在
2. 模块职责清晰
3. `lib.rs` 只做接入，不做主体实现

验收结论：

- `通过`

证据：

- 已新增 `crates/runtime-core/src/repo_context.rs`。
- 该模块集中实现了 Git 检测、repo root 识别、默认分支候选、dirty 状态、最近提交摘要、说明文件白名单命中与轻量摘要、metadata 装配。
- `crates/runtime-core/src/lib.rs` 仅保留 `load_repo_context` 调用与 metadata 接入，不承载采集细节。

### 8.2 数据结构验收

必须确认已形成或等价形成以下结构：

1. `RepoContextSnapshot`
2. `GitSnapshot`
3. `GitCommitSummary`
4. `WorkspaceDocSummary`

通过标准：

1. 结构化对象存在
2. 字段语义清晰
3. 支持降级和空值场景

验收结论：

- `通过`

证据：

- 在 `crates/runtime-core/src/contracts.rs` 中已新增上述 4 个结构体。
- `RepoContextSnapshot` 包含 `workspace_root`、`repo_root`、`git_available`、`git_snapshot`、`doc_summaries`、`warnings`、`collected_at`。
- `GitSnapshot` 支持 `current_branch`、`default_branch` 为空，满足非 Git 和 Git 局部失败降级场景。
- `WorkspaceDocSummary` 控制为路径、类型、是否存在、摘要、是否截断，不回流全文。

### 8.3 合同同步验收

必须确认以下文件已同步：

1. `crates/runtime-core/src/contracts.rs`
2. `gateway/internal/contracts/contracts.go`
3. `frontend/src/shared/contracts.ts`

通过标准：

1. 三端字段语义一致
2. 不存在临时兼容字段漂移

验收结论：

- `通过`

证据：

- Rust 合同已新增 `RepoContextSnapshot`、`GitSnapshot`、`GitCommitSummary`、`WorkspaceDocSummary`。
- Go 合同已同步对应结构，确保 runtime 响应结构一致。
- Frontend TypeScript 合同已同步对应类型，前端可安全消费最小 repo context 字段。

---

## 9. 行为验收

本节验证功能是否按预期工作。

### 9.1 场景 A：标准 Git 仓库

测试环境：

1. 使用 `D:\newwork\m1_acceptance_ascii\git-standard`
2. 该目录为正常 Git 仓库
3. 存在 `AGENTS.md`、`README.md`、`docs/README.md`
4. 当前分支为 `main`
5. 人工追加 `README.md` 变更以制造 dirty 状态

操作步骤：

1. 启动 `target/debug/runtime-host.exe`
2. 向 `/v1/runtime/run` 发送 `list:` 请求
3. 观察 `analysis_ready` 与 `run_finished` 事件 metadata

必须看到：

1. 当前工作区根路径
2. 当前分支
3. dirty 状态
4. 至少一个说明文件命中结果

通过标准：

1. run 正常执行
2. repo context 信息可见
3. 信息不是空壳字段

验收结论：

- `通过`

证据：

- 响应文件：`D:\newwork\m1_acceptance_ascii\git-standard-response.json`
- `analysis_ready.metadata.current_branch = main`
- `analysis_ready.metadata.default_branch = main`
- `analysis_ready.metadata.git_dirty = true`
- `analysis_ready.metadata.doc_hits = 3`
- `analysis_ready.metadata.doc_paths = AGENTS.md / README.md / docs/README.md`
- `analysis_ready.metadata.repo_context_status = ready`
- `analysis_ready.detail` 已体现“基于当前仓库状态，识别到分支 main，存在未提交修改，并命中说明文件”

### 9.2 场景 B：非 Git 目录

测试环境：

1. 使用 `D:\newwork\m1_acceptance_ascii\non-git`
2. 当前工作区不是 Git 仓库
3. 存在 `README.md`

操作步骤：

1. 向 runtime 发送 `list:` 请求
2. 观察事件和返回结果

必须看到：

1. run 不失败
2. repo context 有降级说明
3. Git 信息缺失被正确解释

通过标准：

1. 任务仍可继续
2. 不是静默失败
3. 不是整体报错退出

验收结论：

- `通过`

证据：

- 响应文件：`D:\newwork\m1_acceptance_ascii\non-git-response.json`
- `result.status = completed`
- `analysis_ready.metadata.repo_context_status = degraded`
- `analysis_ready.metadata.repo_context_warnings` 含 `fatal: not a git repository`
- `analysis_ready.metadata.doc_hits = 1`
- `run_finished.metadata.final_answer` 正常返回目录内容，说明主链路未中断

### 9.3 场景 C：Git 局部失败

测试目标：

验证某条 Git 命令失败时的鲁棒性。

操作建议：

1. 使用一个已初始化但没有任何提交的 Git 仓库：`D:\newwork\m1_acceptance_ascii\git-partial`
2. 再发起聊天任务

必须看到：

1. 系统保留可获取的其他信息
2. `warnings` 或等价字段中有说明
3. run 不会整体失败

通过标准：

1. 降级生效
2. 失败可解释

验收结论：

- `通过`

证据：

- 响应文件：`D:\newwork\m1_acceptance_ascii\git-partial-response.json`
- `result.status = completed`
- `analysis_ready.metadata.current_branch = main`
- `analysis_ready.metadata.repo_root = D:/newwork/m1_acceptance_ascii/git-partial`
- `analysis_ready.metadata.repo_context_status = degraded`
- `analysis_ready.metadata.repo_context_warnings` 含 `fatal: your current branch 'main' does not have any commits yet`
- 说明 Git `log` 失败时，repo root、当前分支、dirty 状态和说明文件命中仍然保留

### 9.4 场景 D：多个说明文件命中

测试环境：

1. `git-standard` 仓库包含 `AGENTS.md`
2. `git-standard` 仓库包含 `README.md`
3. `git-standard/docs/README.md` 存在

操作步骤：

1. 发起聊天任务
2. 检查 repo context 中的说明文件摘要

必须看到：

1. 多个说明文件被发现
2. 每个摘要长度受控
3. 没有全文回流

通过标准：

1. 白名单发现规则生效
2. 摘要是轻量结果，不是全文复制

验收结论：

- `通过`

证据：

- `analysis_ready.metadata.doc_hits = 3`
- `analysis_ready.metadata.doc_paths` 精确命中 3 个白名单文件
- `crates/runtime-core/src/repo_context.rs` 中摘要读取上限为 `4096` 字节，并通过 `summarize_text` 做轻量截断
- 事件 metadata 只回流 `doc_hits`、`doc_paths`、`repo_context_summary`，未回流原文全文

### 9.5 场景 E：planner 接入

测试目标：

验证 repo context 不只是展示，而是真正进入 runtime 主链路。

观察项：

1. `analysis_ready` 或等价事件中是否体现 repo context 痕迹
2. planner 分析摘要是否体现仓库状态感知

通过标准：

1. planner 不是完全无感
2. 至少能体现“基于当前仓库状态”的分析语义

验收结论：

- `通过`

证据：

- `crates/runtime-core/src/planner.rs` 的 `analysis_summary` 已新增 `repo_context: &RepoContextSnapshot` 参数
- 标准 Git 场景 `analysis_ready.detail` 已包含“基于当前仓库状态，运行时识别到分支 `main`，存在未提交修改。已命中高价值说明文件……”
- 非 Git 场景 `analysis_ready.detail` 已包含“当前工作区不在 Git 仓库中，分析将仅依赖工作区路径与说明文件”

### 9.6 场景 F：前端展示

测试目标：

验证前端展示满足“最小可用”，但不破坏 V2 页面结构。

必须确认：

1. 右侧上下文栏可以展示最小 repo context
2. 首页结构仍保持 `Top Bar + Main Chat + Context Sidebar + Bottom Panel`
3. 没有新增复杂面板或一级导航

通过标准：

1. 展示可读
2. 信息不过量
3. 页面结构稳定

验收结论：

- `通过`

证据：

- `frontend/src/workspace/ContextSidebar.tsx` 已新增“仓库上下文”卡片
- 该卡片展示 `workspace_root`、`repo_root`、`current_branch`、`default_branch`、`git_dirty`、`doc_hits`、聚合摘要、命中文件路径与降级说明
- 未新增一级导航或新页面，仍保持原有 V2 页面骨架

---

## 10. 性能与资源验收

M1 虽然不是性能专项，但必须证明没有明显违背项目路线。

### 10.1 内存占用验收

必须确认：

1. 没有把说明文件全文常驻在大对象中
2. 没有把多份原文重复写入 session 或 event
3. 没有新增 Python 或其他额外长驻依赖

验收结论：

- `通过`

证据：

- `repo_context.rs` 仅按白名单读取少量固定文件，单文件读取上限 `4096` 字节
- metadata 仅回流 `repo_context_summary`、`doc_hits`、`doc_paths` 等最小字段
- session 与 event 中未新增 README 全文
- 技术栈仍为 Go + Rust + 现有前端，无新增正式后端运行时依赖

### 10.2 响应延迟验收

建议观察：

1. M1 接入后聊天任务是否仍保持可接受响应速度
2. repo context 采集是否引入明显卡顿

通过标准：

1. 无明显体感阻塞
2. 如存在轻微增加，可解释且可接受

不通过情形：

1. 普通 run 被显著拖慢
2. 每次任务都出现明显等待

验收结论：

- `通过`

证据：

- 黑盒验收中三个场景均在单次请求内快速返回，无超时
- `cargo build` 后本地 runtime 自测场景响应均在秒级内完成，未观察到显著卡顿
- 采集逻辑未引入后台任务与大缓存

### 10.3 资源路线验收

必须确认：

1. M1 仍完全建立在 Go + Rust 主线上
2. 未新增 Python 正式依赖
3. 未新增重型后台服务

验收结论：

- `通过`

证据：

- Rust 负责 repo context 采集与装配
- Go 只同步合同与桥接
- 前端只消费 metadata 做右侧栏展示
- 未新增顶层 API endpoint、后台服务或其他运行时依赖

---

## 11. 降级场景验收

M1 一个非常重要的验收点不是“都成功”，而是“失败是否可控”。

### 11.1 可接受的降级

以下情况允许作为可接受降级：

1. Git 不存在
2. 当前目录非 Git 仓库
3. 个别说明文件不存在
4. 个别 Git 字段无法获取

前提是：

1. 系统继续运行
2. 有明确说明
3. 主链路不被中断

### 11.2 不可接受的降级

以下情况不能被归类为“只是降级”：

1. run 整体失败
2. planner 完全拿不到任何上下文却没有说明
3. 前端展示空白但没有解释
4. 合同不同步导致前端报错

验收结论：

- `通过`

证据：

- 非 Git 场景：`repo_context_status = degraded`，warning 明确说明 `not a git repository`，run 继续完成
- Git 局部失败场景：`repo_context_status = degraded`，warning 明确说明 `main does not have any commits yet`，run 继续完成
- 当前真实工作区（`D:/newwork/本地智能体`）本身非 Git 仓库时，也能稳定返回目录列举结果和降级说明

---

## 12. 偏差与遗留问题记录

验收时必须记录真实偏差，不能为了推进里程碑而忽略。

### 12.1 偏差记录表

请逐条记录：

1. 偏差描述：前端右侧栏当前展示 repo context 的最小聚合信息，但未逐条展开每份说明文件的摘要正文
2. 影响范围：仅影响右侧栏的信息细粒度，不影响 runtime 采集、planner 接入、metadata 回流与最小展示
3. 是否阻断 M1 通过：`不阻断`
4. 建议在哪个里程碑修复：`M2，如需在 Context Envelope 中统一 doc summary 呈现方式，可一并调整`

记录：

- 已记录上述 1 条偏差，作为“有条件通过”的唯一非阻断问题

### 12.2 已知遗留问题

请记录：

1. 当前未解决但可接受的问题
2. 是否建议进入 M2 前先修复

记录：

1. 当前工作区 `D:/newwork/本地智能体` 本身不是 Git 仓库，因此实际 UI 中默认只会看到 repo context 降级展示；这不是实现缺陷，而是当前工作区状态导致
2. 说明文件摘要正文目前未在右侧栏逐条展示；不建议阻塞 M2，可在 M2 统一上下文装配阶段一并优化

---

## 13. 最终判定

### 13.1 判定清单

请逐项确认：

1. 是否完成独立 `repo_context` 模块：`是`
2. 是否形成结构化 `RepoContextSnapshot`：`是`
3. 是否完成 run 前采集：`是`
4. 是否完成 planner 最小接入：`是`
5. 是否完成事件 metadata 回流：`是`
6. 是否完成前端最小展示：`是`
7. 是否完成降级处理：`是`
8. 是否保持 Go + Rust 主线：`是`

记录：

- 以上 8 项全部满足

### 13.2 最终结论

请选择：

1. `通过`
2. `有条件通过`
3. `不通过`

最终判定：

- `有条件通过`

### 13.3 是否允许进入 M2

请选择：

1. `允许直接进入 M2`
2. `修复少量问题后进入 M2`
3. `不允许进入 M2`

结论：

- `允许直接进入 M2`

说明：

1. 本轮唯一遗留问题不阻断 M2
2. 不存在会推翻 M1 结构设计的关键缺陷
3. 满足你提出的规则：只有在验收文档判定为“通过”或“有条件通过且问题不阻断 M2”时，才允许进入下一阶段；本轮属于后一种情况

---

## 14. 给规划对话的回传要求

当这份文档填写完成后，如果要继续找规划对话细化 M2，必须连同以下材料一起提供：

1. 本文档的完整填写版
2. M1 实现说明
3. 修改文件清单
4. 构建与测试结果
5. 当前剩余问题列表

本轮已具备以下回传材料：

1. 本文档已完整填写
2. 修改文件清单如下：
   - `crates/runtime-core/src/repo_context.rs`
   - `crates/runtime-core/src/contracts.rs`
   - `crates/runtime-core/src/lib.rs`
   - `crates/runtime-core/src/planner.rs`
   - `crates/runtime-core/src/paths.rs`
   - `gateway/internal/contracts/contracts.go`
   - `frontend/src/shared/contracts.ts`
   - `frontend/src/workspace/ContextSidebar.tsx`
3. 构建结果：
   - `cargo build` 通过
   - `go build ./...` 通过
   - `npm run build` 通过
4. 测试结果：
   - ASCII 标准 Git 场景通过
   - ASCII 非 Git 场景通过
   - ASCII Git 局部失败场景通过
   - 中文路径工作区实测可正常列目录并稳定降级
5. 剩余问题列表：
   - 前端未逐条展示说明文件摘要正文，判定为非阻断问题
