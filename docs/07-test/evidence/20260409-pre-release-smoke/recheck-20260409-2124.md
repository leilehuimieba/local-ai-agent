# 30 分钟小回归复跑记录（2026-04-09 21:24）

## 本次复跑范围

1. 学习问句混合路由
2. provider 波动场景
3. 页面分层抽检（answer / recovery / system）
4. 最小构建与路由单测

## 运行样本抽检（来自 run-events）

1. `run-1775740287676-72`：`verification_completed/run_finished` 都是 `result_mode=answer`，工具 `session_context`
2. `run-1775740313939-75`：`verification_completed/run_finished` 都是 `result_mode=recovery`，工具 `project_answer`
3. `run-1775740821016-81`：`verification_completed/run_finished` 都是 `result_mode=recovery`，工具 `session_context`（provider 不可达恢复）
4. `run-1775740796749-78`：`verification_completed/run_failed/run_finished` 都是 `result_mode=system`（模式阻止）

## 页面分层证据位置

1. `answer` 页面截图：`../20260409-result-mode-runtime-ui-closure/answer.page.png`
2. `recovery` 页面截图：`../20260409-result-mode-runtime-ui-closure/recovery.page.png`
3. `system` 页面截图：`../20260409-result-mode-runtime-ui-closure/system.page.png`
4. 对应事件导出：`../20260409-result-mode-runtime-ui-closure/*.run-events.json`

## 本次复跑命令与结果

1. `cargo build -p runtime-core`：通过
2. `cargo build -p runtime-host`：通过
3. `frontend npm run build`：通过
4. `cargo test -p runtime-core plans_context_answer_for_learning_status_questions`：通过
5. `cargo test -p runtime-core plans_project_answer_for_status_questions_with_material`：通过

