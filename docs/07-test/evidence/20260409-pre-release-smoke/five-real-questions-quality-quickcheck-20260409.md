# 5 条真实用户问句快测（回答质量）

执行时间：`2026-04-09 21:29` 到 `2026-04-09 21:30`  
执行方式：走真实网关 `POST /api/v1/chat/run`，每条等待到 `run_finished / run_failed`  
原始结果：`five-real-questions-raw-20260409-quick.json`

## 评分口径（务实版）

每条按 3 项打分（每项 1 分）：

1. 是否直接回答了用户问题
2. 是否给出可执行下一步
3. 是否清晰可读（不绕、不过度跑题）

判定：

1. `2-3` 分：通过
2. `0-1` 分：不通过

## 样本结果

| 样本 | run_id | result_mode | 质量分 | 结论 | 说明 |
|---|---|---|---:|---|---|
| Q1 | `run-1775741355130-84` | `recovery` | 1 | 不通过 | 仅给出“主回答失败+补充上下文”，未回答“已完成/未完成/下一步” |
| Q2 | `run-1775741364093-87` | `system` | 0 | 不通过 | 触发多轮工具后超轮次，未给出 30 分钟执行清单 |
| Q3 | `run-1775741374348-90` | `answer` | 2 | 通过 | 给了 3 个判断信号和排查建议；不足是偏通用、略长 |
| Q4 | `run-1775741386614-93` | `recovery` | 1 | 不通过 | 仅恢复口径，未输出“今天就能执行的最小方案” |
| Q5 | `run-1775741390826-96` | `system` | 0 | 不通过 | 触发工具循环后 `run_failed`，无有效收口顺序答案 |

## 快测结论

1. 本轮 5 条真实问句里，回答质量“通过”是 `1/5`。
2. 主要问题不是页面分层，而是“自然问句被路由到 agent_resolve 并进入工具循环”以及“provider 波动导致 recovery 空答复”。
3. 这说明当前系统在“回答质量稳定性”上仍有真实风险，尤其是无上下文的新会话问句。

## 风险分层（仅记录，不扩开发）

1. 高风险：`Q2/Q5` 出现 `verification_failed + 达到最大执行轮次`，用户拿不到答案。
2. 中风险：`Q1/Q4` 在 provider 波动时只给恢复说明，内容价值低。
3. 低风险：`Q3` 可用，但项目贴合度和长度还可以继续优化。

